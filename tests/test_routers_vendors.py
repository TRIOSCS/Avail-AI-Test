"""
tests/test_routers_vendors.py — DEPRECATED: Split into domain-specific files

All 197 tests have been moved to:
- tests/test_routers_vendors_crud.py     (104 tests) — vendor CRUD, reviews, autocomplete, helpers
- tests/test_routers_vendor_contacts.py  (42 tests)  — contact lookup, CRUD, bulk, email metrics
- tests/test_routers_materials.py        (34 tests)  — material card CRUD, enrich, import-stock, merge
- tests/test_routers_vendor_analytics.py (17 tests)  — offer history, confirmed offers, parts summary

This file is intentionally empty. Delete it once the split is confirmed stable.
"""
