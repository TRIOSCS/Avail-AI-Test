No duplicates. However, this isn't really a bug — the test prompt's assumption about a standalone Upload section is incorrect. The upload feature works as designed within the requisitions context.

**PASS: upload**

**Summary:**
- There is no standalone "Upload" section in the sidebar or as a separate view
- Upload functionality exists and is integrated into the requisitions workflow:
  - CSV/Excel bulk upload button on each requisition row
  - File attachment upload within requisition detail views
- This is by design, not a bug
- **Pre-existing console errors** observed on every page load (not upload-specific):
  - `manifest.json` returns 404
  - Service worker registration fails (script returns 404)
  - `favicon.ico` returns 404
