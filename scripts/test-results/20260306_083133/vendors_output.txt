## Vendors Test Summary

### Tests Passed
- **Test 1: Vendor list loads** - PASS. 6,076 vendors load with proper columns (name, tier, score, response rate, POS sent, parts seen, last activity).
- **Test 2: Search vendors** - PASS. Search filters correctly (e.g., "Digi" returns 4 matching vendors). Unicode search works. Edge cases (empty, special chars, very long input) handled properly. No SQL injection vulnerability.
- **Test 3: Vendor detail view** - PASS. Detail endpoint returns complete vendor data including brands, contacts, tags.
- **Test 4: Sighting counts** - PASS. All sighting counts are valid integers (no NaN, no negatives). 
- **Test 7: Scorecard data** - PASS. Performance/scorecard endpoint returns structured data with response_rate, quote_conversion, trend data.
- **Test 9: Contact info** - PASS. Contacts return readable names, emails, phone numbers, titles. No ciphertext or encoded data.
- **Test 12: Pagination** - PASS. Offset/limit pagination works correctly through the full 6,076 vendor list.

### Tickets Filed (4 bugs)

| Ticket | Issue |
|--------|-------|
| **TT-20260306-011** | **Sort is broken** - `sort=name&order=desc` and `sort=name&order=asc` return identical results. `sort=sighting_count&order=desc` returns unsorted data (0,0,0,23,0). |
| **TT-20260306-012** | **Tier filter is broken** - `tier=proven`, `tier=caution`, `tier=dev` all return the same 6,076 vendors. Filter chips have no effect. |
| **TT-20260306-014** | **Data inconsistency** - Vendor 5159 detail says `vendor_score=20.1, is_new=false` but engagement endpoint says `vendor_score=null, is_new=true`. |
| **TT-20260306-018** | **Duplicate vendors + non-vendor entries** - Two Digi-Key entries (IDs 5159 & 975) with same domain but different normalized names. Non-component vendors (1Password, Nikkei Asia) pollute the list. |

### Observations (not bugs)
- Most vendors (5,900+ of 6,076) have 0 sightings and null scores - the list is dominated by unscored new vendors
- Console errors are cosmetic only (missing favicon.ico and manifest.json - PWA setup incomplete)
- Browser testing was limited due to inability to set session cookies (needed `browser_evaluate` permission) — all deep testing done via API
