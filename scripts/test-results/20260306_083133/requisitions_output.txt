## Requisitions Test Results Summary

### PASS (no issues):
- **Test 1**: Requisition list loads — 7 requisitions returned with complete data
- **Test 2**: Requisition detail/requirements — 6 parts returned for ID 23433, all data readable
- **Test 3**: Parts data integrity — MPNs readable, quantities numeric, prices proper floats/null, brands readable
- **Test 5**: Sightings data — vendor names readable, MPNs readable, quantities numeric
- **Test 7**: Pagination — offset/limit work correctly, no duplicate IDs across pages
- **Test 8**: Requirement counts match between list (6 parts) and requirements endpoint (6 items) for ID 23433

### BUGS FOUND (3 tickets filed):

1. **TT-20260306-013** — **Status filter on requisitions API does not filter by specific status**
   - `?status=active` and `?status=draft` both return all 7 requisitions
   - Backend only handles `status=archive` vs everything else; ignores specific status values

2. **TT-20260306-015** — **Deadline field accepts freeform text (ASAP) — mixed type date/string**
   - IDs 23438 and 23437 have `deadline="ASAP"` instead of a date
   - Can cause client-side `Invalid Date` errors

3. **TT-20260306-021** — **Draft requisitions missing from /api/requisitions/counts**
   - `/counts` reports open=5, but list shows 7 items (5 active + 2 draft)
   - Draft status not included in any count category

### BLOCKED:
- **Browser UI tests** — Could not authenticate in headless browser (Microsoft OAuth requires interactive login, no agent session cookie mechanism exists). Console errors for manifest.json and favicon.ico already reported in existing tickets.
