# Deep Abandoned-Work Sweep — Report (2026-07-16)

_10-lane deep sweep; **9 lanes completed** before the workflow hung. **39 candidates.** ⚠️ The adversarial-verify + dedup phases were cut off by the hang, so these are **lane-found, not yet independently verified** — treat as high-quality leads to confirm, not confirmed facts. Excludes the prior 17-item report + tracked backlog._

## Headline

39 candidates: 4 merged-but-unreachable, 9 never-built, 4 partially-built, 1 built-not-merged, 10 dead-code, 6 dead-data, 3 disabled-test, 1 flag-off-unwired, 1 stale-claim.

**Biggest theme:** the *same* 'shipped but no way to reach it' failure that started this whole thread recurs **4 more times** (whole review queues built with no nav), plus supplier connectors you explicitly asked for that were never built, and a large tail of dead/legacy code left mounted after reworks.


## Merged but unreachable (built, no UI entry point) (4)

### offer-review-queue-unreachable  ·  _orphaned-routes_
- **What:** The global 'review queue' console for medium-confidence AI-parsed vendor offers (PENDING_REVIEW) — a purpose-built cross-requisition triage page with promote/reject actions — has no nav item, badge, or link anywhere. Users can't find it.
- **Evidence:** Route app/routers/htmx/offers/crud.py:718 offer_review_queue renders app/templates/htmx/partials/offers/review_queue.html (queries Offer.status==OfferStatus.PENDING_REVIEW, crud.py:729; promote/reject gated on AccessKey.APPROVE_OFFERS at crud.py:744). Sibling JSON API app/routers/crm/offers.py:940 list_review_queue (/api/offers/review-queue) also orphaned. grep of app/templates + app/static/*.js for 'review-queue'/'review_queue' returns ONLY self-references inside review_queue.html — no nav_items entry in mobile_nav.html:25-46, no alert badge, no requisition-tab link. Live staging DB: SELECT status,count(*) FROM offers → pending_review=451.
- **Impact:** A reviewer with APPROVE_OFFERS cannot open the built queue of all AI-flagged offers awaiting sign-off; the cross-requisition review workflow is invisible even though 451 offers currently sit in it on staging.
- **Why abandoned:** Offers reach PENDING_REVIEW via ai_offer_service.py:191 / response_parser.py:300 (AI parse confidence 0.5-0.8). They are reviewable one-at-a-time inside each requisition's offers tab (offers.html:207-218 → /v2/partials/requisitions/{id}/offers/{id}/review), so the dedicated global console was built as a cross-cutting triage view and then simply never got a nav slot. Template last touched by design-pass #706 and P5.1 (still maintained), confirming it is live code with a dropped entry point, not a deleted feature. 451 flagged offers on staging with no cross-requisition way to see them.

### follow-ups-queue-unreachable  ·  _orphaned-routes_
- **What:** The 'Follow-ups' queue — chase stale RFQ email replies, with per-contact send, AI-draft, and batch-send — is a working page reachable only by typing /v2/follow-ups. A live amber count badge shows how many replies need chasing, but nothing links to the actual queue.
- **Evidence:** Page route app/routers/htmx_views.py:153 (/v2/follow-ups → v2_page shell → else-branch loads /v2/partials/follow-ups). Workspace served at app/routers/htmx/offers/follow_ups.py:88 (list), :179 send, :223 ai-draft, :258 send-batch. The ONLY UI reference is the count badge at app/templates/htmx/partials/shared/mobile_nav.html:95-101 (hx-get /v2/partials/follow-ups/badge, follow_ups.py:338), which is a <span> counter INSIDE the requisitions <a href="/v2/requisitions"> — tapping it navigates to the Sales Hub, not follow-ups. grep of templates+static for '/v2/follow-ups' and '/v2/partials/follow-ups' (excluding /badge) = 0. follow_ups/list.html is not {% include %}'d by any reachable workspace.
- **Impact:** Users see 'N follow-ups to chase' on the Sales Hub icon but have no way to open the follow-up queue; the individual + batch reply-chasing workflow is effectively unreachable.
- **Why abandoned:** Same pattern as the already-tracked Buy Plans hub nav gap: the feature (and its polled badge) shipped but no nav destination was added for the page itself. Files current as of the 07-09 offers-router split (P4.3), so it is live code missing only its entry point.

### unmatched-activity-review-and-click-to-call-unwired  ·  _orphaned-routes_
- **What:** The CRM 'unmatched activities' review queue (call/email touches the system couldn't auto-attribute to a contact, so a human assigns or dismisses them) and the 8x8 click-to-call 'initiate call' action are built server-side but have no UI.
- **Evidence:** app/routers/v13_features/activity.py:515 list_unmatched_activities (/api/activities/unmatched), :223 initiate_call_endpoint (/api/calls/initiate), plus /api/activities/{id}/attribute, /api/activities/{id}/dismiss, /api/activities/call, /api/activities/email. grep of app/templates + app/static/*.js for 'activities/unmatched','calls/initiate','api/activities/*attribute|dismiss' = 0 (only unrelated 'unmatched' token in resell/_offers.html). The reverse consumer sweep (all hx-*/fetch/ajax URLs in templates+JS) confirms no dynamic base builds these. Router docstring lines 3-4 explicitly describe 'unmatched queue, activity attribution/dismissal' as intended UI.
- **Impact:** Activities that can't be auto-linked to a contact accumulate with no review surface to attribute/dismiss them, and the click-to-call capability can't be invoked from the app.
- **Why abandoned:** The webhooks in the SAME router (/api/webhooks/graph|teams|acs) are wired and feed activity ingestion, but the human-facing attribution queue + click-to-call front-end was never built — a half-shipped slice of the CRM call/email-tracking program. File last functionally changed 2026-07-09; endpoints have zero UI and zero internal callers.

### cross-req-buyer-lead-queue-unwired  ·  _orphaned-routes_
- **What:** A cross-requisition 'buyer follow-up' leads queue (all SourcingLeads filterable by buyer_status, with per-lead status transitions and thumbs-up/down feedback) is exposed as a JSON API but has no front-end; leads never leave buyer_status='new'.
- **Evidence:** app/routers/requisitions/requirements.py:887 leads_queue (/api/leads/queue, 'Cross-requisition buyer follow-up queue', response_model list[LeadOut]), :908 get_lead_detail, :931 patch_lead_status (buyer_status), :972 add_lead_feedback. grep of templates+static for '/api/leads','leads/queue','buyer_status','lead*feedback' = 0. The visible leads UI is per-requisition only (app/templates/htmx/partials/sourcing/lead_*.html → /v2/partials/sourcing/leads/{id}, served by app/routers/htmx/sourcing.py:92/238/308/617), a different endpoint family. Live staging DB: SELECT buyer_status,count(*) FROM sourcing_leads → new=418 (every lead stuck at 'new').
- **Impact:** There is no place for a buyer to work leads across requisitions or record lead-quality feedback; the buyer_status pipeline the backend supports is inert.
- **Why abandoned:** The JSON buyer-workflow API (queue + status machine + feedback signal) was built alongside the per-requisition sourcing-leads HTMX views but its cross-cutting queue front-end was never wired, so buyer_status/feedback are write paths nothing calls — 418 leads sit unadvanced on staging.


## Never built (requested, no trace in code) (9)

### proactive-rematch-on-offer-approval  ·  _code-markers_
- **What:** Vendor offers that are still pending manager approval when the proactive matching scan runs, and are approved afterward, are silently never proactively matched to customers — the broker never sees the match and the sales lead is lost.
- **Evidence:** app/services/proactive_matching.py:476-478 documents the gap verbatim ('an offer created as pending_review (excluded here) that is later approved won't be re-scanned because the watermark has already advanced past its created_at. The proper fix is a re-match hook triggered on offer approval (follow-up task).'). run_proactive_scan filters Offer.created_at > watermark AND Offer.status in (ACTIVE, APPROVED) at proactive_matching.py:479-490. The offer-approval endpoint app/routers/htmx/offers/crud.py:346-354 sets status=APPROVED but never calls find_matches_for_offer. find_matches_for_offer has exactly two callers (purchase_history_service.py:189 import backfill; proactive_matching.py:504 batch scan) — neither fires on approval.
- **Impact:** Silent loss of proactive sales matches for any offer that required approval — likely a large fraction, since PENDING_REVIEW is a normal initial offer state (status_machine.py:25).
- **Why abandoned:** The comment explicitly labels the fix a deferred 'follow-up task.' I traced every caller of find_matches_for_offer and the full offer approve/promote path on main — no re-match hook exists anywhere, and the watermark permanently excludes the pending-then-approved offer. Not present in the 17-item report or the tracked roadmap/backlog (grepped).

### nightly-suite-crash-class-and-real-alerting  ·  _code-markers_
- **What:** The nightly full-test-suite run has no real alerting: a FAIL or xdist CRASH only appends an 'ALERT:' line to a per-day log file nobody watches, so a broken nightly suite rots silently. Root-causing the xdist worker-crash class was also deferred.
- **Evidence:** scripts/nightly_tests.sh:70-73 — non-PASS status just `echo "ALERT: ${STATUS}" >> "$LOG_FILE"` (to /var/log/avail/nightly_tests/); no email/Slack/webhook/GH-issue anywhere. Merged PR #634 body: 'Remaining W3 (proper wave): the xdist worker-crash class itself + real alerting.' grep of .github/ and scripts/ finds no notification mechanism.
- **Impact:** Nightly regressions/coverage drops/worker crashes go unnoticed until someone manually reads the log; the 'alerting' is a dead write.
- **Why abandoned:** Explicitly promised as a remaining 'proper wave' in merged PR #634 (which only fixed the honest crash classification + clean-worktree pinning). Never delivered; no alerting integration exists on main.

### enrichment-review-queue-never-wired  ·  _dead-data_
- **What:** The human-in-the-loop enrichment review queue (accept/reject proposed enrichment field-values) plus its batch-job tracker were fully scaffolded in the schema but never wired — no code ever writes a proposed enrichment to review, and there is no review UI.
- **Evidence:** Models app/models/enrichment.py:23 (EnrichmentJob, 'Tracks bulk enrichment runs') and :50 (EnrichmentQueue, 'Pending enrichment results for review or auto-apply', 7 indexes, polymorphic vendor_card/company/vendor_contact target + status/reviewed_by review workflow). Tables created in alembic/versions/001_initial_schema.py. Repo-wide the ONLY references are the re-exports app/models/__init__.py:51-52 and FK-repoint bookkeeping in app/services/vendor_merge_service.py:79,81 and company_merge_service.py:153. Zero instantiations, zero queries, zero templates/routes. Live DB: enrichment_queue=0 rows, enrichment_jobs=0 rows (direct count).
- **Impact:** The promised 'review proposed enrichment before it is applied' workflow does not exist; enrichment either auto-applies or is dropped, with no queue for a human to accept/reject, and no batch-job progress tracking surface.
- **Why abandoned:** I grepped every app/, scripts/, jobs/, scheduler path for writers (instantiation, session.add, Core insert, generic model= helpers) and found none; the only mentions are defensive FK-repoint entries in the merge services, which means the code 'knows' these tables could hold rows but nothing ever populates them. No review_queue template exists for enrichment (the one review_queue.html is under offers/, an unrelated live feature). This is day-1 scaffolding whose writer and reviewer UI were never built.

### facet-audit-accuracy-harness-never-built  ·  _dead-data_
- **What:** The Phase-2.2 volume-weighted facet-accuracy audit harness — records a correct/wrong/unverifiable verdict per decoded material-spec facet — has its table, CHECK constraint and validator shipped, but the writer that performs the audits was never built.
- **Evidence:** app/models/telemetry.py:45 FacetAudit (table facet_audits, closed verdict vocabulary + ck_facet_audits_verdict CHECK + @validates guard) created in alembic/versions/104_trust_telemetry.py. The model's own docstring (telemetry.py:10-12) names the writer: 'the Phase-2.2 audit harness (app/management/audit_facets.py, future) writes FacetAudit'. That file does not exist (ls app/management/audit_facets.py -> No such file). Zero FacetAudit instantiations anywhere; only reference outside the model is the __init__.py:178 export. Live facet_audits=0 rows.
- **Impact:** There is no facet-accuracy audit capability; the decoded-spec-facet trust reporting the table was built to support cannot run, so facet accuracy is unmeasured despite the 55,409-row material_spec_facets table it was meant to audit.
- **Why abandoned:** The migration was shipped ahead of the harness so 'Phase 2.2's audit harness needs no second migration' (docstring), but the harness (audit_facets.py) was never created — a classic ship-the-schema-first-then-abandon trace. The sibling ReconcileRun in the same migration DID get its writer (reconcile_decoded_facets.py), FacetAudit did not.

### browser-e2e-suite-runs-in-no-automated-context  ·  _disabled-tests-ci_
- **What:** The entire browser/end-to-end layer — a Python pytest-playwright suite plus a separate TypeScript Playwright suite — runs in no CI job, no deploy step, and not in the nightly host suite, despite recent investment to make it deterministic and CI-ready.
- **Evidence:** Two unrun suites: (1) tests/e2e/*.py (7 test files: test_app_deep, test_core_pages_render, test_deep_dive, test_navigation_smoke, test_connectors_settings_e2e, test_sightings_workspace_e2e, test_spec_code_resolver_e2e) — ignored by pytest.ini addopts (--ignore=tests/e2e), excluded by scripts/ci_shard.py:38, and the nightly host runner scripts/nightly_tests.sh:41 runs `"$PYTEST" tests/` which honors that ignore. (2) e2e/*.spec.ts (10 specs incl. api, auth, smoke, workflows, dead-ends, accessibility, visual, materials-ui, sales-hub-ui, data-validation) driven by playwright.config.ts, whose webServer auto-starts its own uvicorn (command line 23, reuseExistingServer:false) so it is fully CI-runnable. grep of .github/, deploy.sh, scripts/ for playwright/npx/tests/e2e finds only a ci_shard exclusion, a security.yml comment, and an assertion-theater baseline entry — no runner. deploy.yml and security.yml run no test suite. Commit b4f61927 (2026-07-05) built a seed_e2e_data fixture + core_guard + tests/e2e/test_core_pages_render.py ('a miss FAILS instead of skips') to make the Python e2e suite deterministic, and added the postgres-paths CI job in the same commit — but never added an e2e CI job.
- **Impact:** Every navigation/auth/workflow/accessibility/visual regression the browser suites were built to catch ships unguarded; the substantial deterministic-seed work is stranded and rots (skips already creeping in per tests/e2e/*.py pytest.skip calls). No dead-end / auth-before-redirect / core-page-render regression can fail a PR.
- **Why abandoned:** The July-5 commit clearly intended to graduate the e2e suite into CI (deterministic seed built, self-starting webServer already present) and even landed a sibling CI job in the same change, but the e2e runner job itself was never wired, so ~17 browser spec files are dark and only run when a human manually invokes pytest tests/e2e/ --headed or npx playwright test. git log -S 'playwright'/'tests/e2e' over .github/workflows shows an e2e job was never added or removed. CLAUDE.md's claim 'the host runs the suite nightly too' is technically true only for the unit suite — nightly_tests.sh inherits the tests/e2e ignore.

### planned-supplier-connectors-never-built  ·  _flags-settings_
- **What:** Six supplier integrations (FindChips/Supplyframe, Future Electronics, Heilind, LCSC, Rochester, Verical/Arrow Marketplace) are advertised to the user in Settings -> Connectors as 'Planned' cards but have no implementation at all.
- **Evidence:** app/services/connector_service.py:67 `_PLANNED = {"findchips","future","heilind","lcsc","rochester","verical"}`; rendered as read-only 'Planned' cards via app/templates/htmx/partials/settings/_connector_macros.html:44 ('Connector on the roadmap — not built yet') and :143-144. Live DB: `SELECT name,status FROM api_sources` returns all six with status='disabled', env_vars=[]. app/services/connector_registry.py get_connector_for_source() has NO branch for any of them (and they are not worker-backed, not enrichment-router sources), so no search/probe path exists.
- **Impact:** Operator sees six named supplier sources promised as 'Planned' on the Connectors page that will never search — no way to enable them, and no backlog item tracks building them.
- **Why abandoned:** They are seeded into the api_sources catalog and given a dedicated UI 'Planned' state + tooltip, i.e. deliberately surfaced to the operator as forthcoming, but no connector class, credential wiring, or search path was ever written. Traced every consumer: only the connectors page renders them; nothing can execute them.

### self-heal-subsystem-configured-but-never-built  ·  _flags-settings_
- **What:** An autonomous self-healing / auto-diagnose subsystem (auto-diagnose issues, auto-execute low-risk fixes, file tickets against a budget, backed by a GitHub repo/token) is fully configured in the deployed staging environment but has zero implementing code in the repo.
- **Evidence:** Staging /root/availai/.env:92-96 sets SELF_HEAL_ENABLED, SELF_HEAL_AUTO_DIAGNOSE, SELF_HEAL_AUTO_EXECUTE_LOW, SELF_HEAL_TICKET_BUDGET, SELF_HEAL_WEEKLY_BUDGET; :114-115 GITHUB_REPO=TRIOSCS/Avail-AI-Test + GITHUB_TOKEN. Whole-repo grep for SELF_HEAL / self_heal (excluding the generic 'self-heals' prose) returns 0 code matches; CHANGELOG.md has no self-heal entry; config.py has no such fields and uses extra='ignore' (config.py:41) so all five are silently dropped.
- **Impact:** The product owner set SELF_HEAL_ENABLED=true (and a $500 weekly / 10-ticket budget) believing an auto-diagnose/auto-fix loop is running; it is a complete no-op — no self-healing occurs and no tickets are filed.
- **Why abandoned:** Five coherent, purpose-built config knobs plus a GitHub integration describe a designed feature, yet no service/job/router consumes any of them. Either the subsystem was scoped (env prepared) and never built, or it is driven by an external agent that is not present in this repo — in both cases nothing in AvailAI acts on the config.

### configurable-sighting-scoring-weights-never-wired  ·  _flags-settings_
- **What:** Six scoring-weight tuning knobs (WEIGHT_RECENCY, WEIGHT_QUANTITY, WEIGHT_VENDOR_RELIABILITY, WEIGHT_DATA_COMPLETENESS, WEIGHT_SOURCE_CREDIBILITY, WEIGHT_PRICE) are set in the deployed .env to make sighting-score weights operator-tunable, but the scorer ignores them and uses a hardcoded weight table.
- **Evidence:** Staging /root/availai/.env:25-30 sets all six WEIGHT_* vars. Whole-repo grep for WEIGHT_* / weight_price etc. returns 0 code matches. app/scoring.py:31 `SIGHTING_V2_WEIGHTS: dict[str,float] = {...}` is a hardcoded 'single source of truth' consumed at scoring.py:117 and :135; no env override path exists. config.py has no WEIGHT_* fields (extra='ignore' drops them).
- **Impact:** Changing any WEIGHT_* value on the server has no effect on sighting scores; an operator tuning them is silently ignored, and the promised 'tune the scoring model' capability doesn't exist.
- **Why abandoned:** The env carries a full set of per-factor weight overrides — clearly intended to externalize the scoring weights — but the capability to read them was never implemented; the weights remain compile-time constants.

### supplier-connectors-future-heilind-rochester-verical  ·  _memory-parked_
- **What:** Four supplier sourcing connectors the user explicitly asked to build — Future Electronics, Heilind, Rochester (direct API) and Verical (browser-worker) — were never built; they exist only as disabled placeholder rows on the Connectors page.
- **Evidence:** Live DB (docker exec availai-db-1 psql) api_sources: future / heilind / rochester / verical all is_active=f, status=disabled; rendered as read-only 'planned' rows via app/services/connector_service.py:74 + L184-186. No connector code exists — ls app/connectors/ and app/services/ match nothing for future|heilind|rochester|verical. Requested in memory project_connectors_rebuild_2026_06_23 ('CONNECTOR BUILD-BACKLOG (user wants these built later — NOT built yet, separate sub-projects): direct connectors for Future Electronics, Heilind, Rochester; browser-workers for The Broker Forum and Verical') and reiterated in project_session_handoff_2026_06_29 ('Future/Heilind/Rochester direct-API connectors; Verical browser-worker').
- **Impact:** Sourcing (the product's core function per CLAUDE.md) cannot query Future, Heilind, Rochester (an authorized EOL/obsolete distributor — genuinely distinct coverage), or the Verical marketplace, so buyers miss availability/pricing from four supplier sources the user specifically wanted wired in.
- **Why abandoned:** Explicitly deferred as 'separate sub-projects' pending API keys (the three direct connectors) or a browser-login capture session (Verical — the same pattern as the shipped TBF/NetComponents/ICSource workers). The Broker Forum (thebrokersite) sibling from that same backlog WAS later built (migration 131, now status=live), leaving these four stranded as placeholder rows plus a memory note. Not carried in the master roadmap or the 17-item report, so it won't resurface on its own.


## Partially built / half-removed (4)

### proactive-matches-unbounded-no-pagination  ·  _code-markers_
- **What:** The Proactive Matches page loads every match for the user in a single unbounded query with no pagination; the fix was explicitly deferred when the N+1 perf fixes shipped.
- **Evidence:** app/services/proactive_service.py:72 `matches = query.all()` — no LIMIT/OFFSET; the page (routers/htmx/proactive.py) renders all of them. Merged PR #662 body: 'The unbounded match query (3rd sub-finding) needs pagination UI, so it's left as a follow-up rather than a silent cap.' Only the joinedload + do-not-offer scoping sub-fixes landed.
- **Impact:** A power user with many open matches loads/renders the entire set on every /v2/proactive hit — slow page + memory pressure that grows with match volume.
- **Why abandoned:** Promised as a follow-up in merged PR #662 (the other two PERF-2 sub-findings shipped, this one did not). Verified still unbounded on main; not in the tracked roadmap/backlog (the only 'pagination' backlog rows are for CSV export and requisitions, unrelated).

### offer-attribution-lifecycle-half-collapsed  ·  _transcripts-deep_
- **What:** The Offer "attribution lifecycle" (active → expired → converted) was decided-against and half-removed, but left in a stuck middle state: the enum was trimmed to a single value while the column, its stale 3-state comment, and a never-rendered serialization all remain — dead-data that always reads "active".
- **Evidence:** app/models/offers.py:108 `attribution_status = Column(String(20), default="active")  # active, expired, converted` (stale comment promises 3 states). app/constants.py:75-78 `AttributionStatus` StrEnum now defines ONLY `ACTIVE = "active"` (the expired/converted members were removed — same cleanup pattern as RiskFlagType, whose docstring says the dead members "were removed"). Sole writer app/routers/htmx/offers/crud.py:507 only ever sets `AttributionStatus.ACTIVE`; no job/service anywhere transitions to expired/converted (grep across app/ = 0 hits). Field is serialized into the offer dict at app/routers/requisitions/requirements.py:1235 but rendered by ZERO templates (grep app/templates = 0). Flagged in docs/superpowers/2026-07-05-unfinished-work-inventory.md line 22 (§🟠 half-built: "attribution_status only ever active … build the transitions or collapse to a boolean") — but NOT carried into docs/superpowers/2026-07-16-master-execution-roadmap.md, which claims to sequence "all outstanding work," and NOT in the 17-item 2026-07-15 report.
- **Impact:** Effectively none today (the field is never displayed), so this is latent dead-data / stale-schema, not a user-visible gap. The concrete residue: a stale inline comment on offers.py:108 that misdescribes the column, a String(20) column that can only ever hold one value (should be dropped or collapsed to boolean), and a serialized-but-unused key in the offer payload. Cleanup is a single small migration + comment fix; alternatively, if per-offer attribution reporting was ever intended, it is the one piece never built.
- **Why abandoned:** Traced from the 2026-07-05 discovery inventory (surfaced in transcript 63825237, which found it as a "decide build-or-drop" half-feature). The team clearly acted on the 07-05 inventory afterward (fixed both dashboard bugs, deleted the dead route-dupes, rewired the contact-enrichment no-op stub, and completed the sibling dead-enum cleanup — RiskFlagType/SourcingType/QualityPlanStatus IN_REVIEW-APPROVED-REJECTED/QPOrderType.REVISION are all now removed), but this one item got only its enum trimmed. The 07-05 inventory's own remedy ("build the transitions OR collapse to a boolean") was never finished — neither branch was taken — and the item then fell out of the 07-16 master roadmap entirely, so it will not resurface on its own.

### teams-qa-knowledge-routing-schema-only  ·  _dead-data_
- **What:** The Teams knowledge Q&A 'routing / delivery / nudge' workflow (deliver a question, nudge for an answer, record how it was answered, enforce a daily-question cap) shipped its DB schema in one migration but the application logic and its config were never built/wired.
- **Evidence:** alembic/versions/064_teams_qa_routing.py:26-28 adds knowledge_entries.{nudged_at, delivered_at, answered_via}; :35-45 creates knowledge_config and seeds ('daily_question_cap','10'). app/models/knowledge.py:86 KnowledgeConfig docstring says 'Called by: services/teams_qa_service.py' — that file does not exist. No code writes nudged_at/delivered_at/answered_via (grep of assignments = none); KnowledgeConfig only appears in __init__.py:101 export; daily_question_cap has zero readers. Live: knowledge_config=0 rows (seed absent), and knowledge_entries.{nudged_at,delivered_at,answered_via} are 100% NULL across 5,211 rows.
- **Impact:** Knowledge questions are never tracked as delivered/nudged, 'answered via' is never recorded, and the daily-question cap is unenforced; the intended Teams Q&A routing behaviour is silently inert.
- **Why abandoned:** The knowledge ledger itself (knowledge_entries, knowledge_service.py, knowledge_jobs.py) is live, but the delivery/nudge/answered-via routing layer added by migration 064 has no writer, no consumer service (teams_qa_service.py missing), and its config row is absent — the feature's schema landed and the logic never followed (or was later removed, leaving orphan columns).

### proactive-approved-offer-not-rematched  ·  _memory-parked_
- **What:** Proactive matching silently drops vendor offers that were approved *after* landing in pending_review — they never surface as customer matches on the Proactive tab.
- **Evidence:** app/services/proactive_matching.py run_proactive_scan (def ~L466) gates to _LIVE_STATUSES=[ACTIVE, APPROVED] but advances a persisted SystemConfig watermark past each offer's created_at; the in-code comment at ~L474-478 states verbatim: 'an offer created as pending_review (excluded here) that is later approved won't be re-scanned because the watermark has already advanced past its created_at. The proper fix is a re-match hook triggered on offer approval (follow-up task).' Grep of app/routers/htmx/offers/* + services for run_proactive_scan / refresh_matches_for_cards on the approval path = none. Flagged deferred in memory project_proactive_tab_2026_06_17 ('pending_review→approved offers missed by created_at watermark; proper fix = re-match hook on offer approval').
- **Impact:** A vendor offer that a buyer reviews and approves (the normal happy path for mined/unsolicited offers) is silently never matched to open customer requirements — the exact buying opportunities the Proactive feature exists to surface can be permanently missed.
- **Why abandoned:** Documented as a deferred 'follow-up task' in the 2026-06-18 proactive-hardening handoff (PR #380) and left as a live in-code TODO marker; the re-match-on-approval hook was never built. Both the scheduled batch scan and the manual POST /v2/partials/proactive/refresh route call run_proactive_scan, which uses the same watermark — so neither ever reconsiders an offer once approved. Not in the 17-item report or the master roadmap. (Its smaller sibling proactive deferrals — engine health monitoring, email-parsed offer pruning, offer-view data quality — were likewise noted 'NOT built'.)


## Built but not on main (1)

### dossier-price-sanity-market-baseline-strip  ·  _git-archive-tags_
- **What:** Part-dossier price-sanity signal — the market panel used to compute a market-median baseline and flag price outliers (offers ~20x+ above the fresh-result median), so buyers were warned when an offer's price was wildly out of line. It was built, shipped, then reverted and never re-landed.
- **Evidence:** Removed from main by commit 7804d47b (PR #401, 'revert(dossier): remove market-baseline strip (price-sanity dropped — data trust)'; branch commit 478f0106) — merge-base confirms 7804d47b IS on main, i.e. the feature is gone. The pre-revert implementation survives ONLY on tag archive/revert/drop-market-baseline-strip: `git show archive/revert/drop-market-baseline-strip:app/search_service.py` still contains `_median()` (line 88) and 'Flag price outliers — historical results 20x+ above fresh median' (line ~773). On current main, grep of app/search_service.py + app/routers/part_dossier.py + app/templates/htmx/partials/search/dossier_market.html for baseline|price.sanity|median|deviation returns nothing feature-related (only an unrelated 'spec deviation' comment at search_service.py:520). No archive branch/PR carries it — only the tag.
- **Impact:** The part dossier surfaces no automatic price-sanity warning. When a supplier offer is priced far out of line with the market (e.g. an order of magnitude above the median of fresh results), nothing flags it for the buyer, so mispriced or stale offers can be acted on without a sanity check.
- **Why abandoned:** Traced every revert across all refs (git log --all --grep=revert) and this is the single reverted feature that was never re-landed. The revert was DELIBERATE — the message frames it as a data-trust decision (the market baseline wasn't reliable enough to flag offers on). But it is structurally identical to the already-tracked vendor reply-ranking PR #444 (both are deliberate data-gated defers whose only surviving ref is an archive tag, revivable once trustworthy source data exists) — and the prior sweep chose to surface #444 as a legitimate 'built-not-merged' backlog row for exactly that reason, so this deserves the same treatment. It ties into the OEM/pricing data-acquisition program in the backlog: if a trustworthy price baseline becomes available, the outlier flag is revivable. No re-add intent exists anywhere in docs/ or memory, and there are zero open refs pointing at it besides the archive tag, so it will not resurface on its own and is at risk of loss on tag GC. Not one of the 17 tracked items nor any excluded item.


## Dead code (orphaned endpoints/models to remove) (10)

### buyplan-pre-c1-approve-fallback-cleanup  ·  _code-markers_
- **What:** The buy-plan approve/reject route keeps a legacy pre-C1 fallback (approve_buy_plan) that its own comment says will be 'removed in a follow-up once no pre-C1 plans remain'; that cleanup never happened.
- **Evidence:** app/routers/htmx/buy_plans.py:705-707 ('If NO open request exists ... we fall back to the legacy approve_buy_plan and log a WARNING (RISK 3, transition window; the fallback is removed in a follow-up once no pre-C1 plans remain).'). The fallback branch is still live at buy_plans.py:743-749.
- **Impact:** Two divergent approval code paths (engine decide vs legacy approve_buy_plan) coexist; the legacy path bypasses the engine's atomic side-effect guarantees and only logs a WARNING, a latent correctness/consistency risk if a pre-C1-shaped plan ever hits it.
- **Why abandoned:** Documented as a transition-window follow-up in the QP Phase C1 approvals migration; no removal landed on main (branch still present). Low-stakes but a genuine dropped cleanup promise — distinct from the excluded 'buy-plan editing' item.

### sync-log-admin-viewer-read-never-written  ·  _dead-data_
- **What:** The admin 'sync logs' viewer endpoint reads a table that nothing ever writes and that no UI ever calls — a dead observability surface that always returns an empty list.
- **Evidence:** app/models/sync.py:11 SyncLog (table sync_logs). Admin endpoint app/routers/crm/enrichment.py:438 GET /api/admin/sync-logs queries db.query(SyncLog) (:448). Repo-wide there is NO writer of SyncLog (no SyncLog(...) instantiation, no session.add, no Core insert in app/, scripts/, jobs/). No template or JS references '/api/admin/sync-logs'. Live sync_logs=0 rows. This is distinct from the actively-written SyncState (app/models/pipeline.py:30), which email_service.py:649 / jobs/email_jobs.py:641,844 / connectors/email_mining.py:212 populate — SyncLog is the orphan.
- **Impact:** An admin who hits /api/admin/sync-logs always sees [] regardless of sync activity; there is no working sync-history view.
- **Why abandoned:** A sync-audit table + admin JSON viewer were built, but the writer that would log each sync run was never implemented (SyncState superseded it for state, but nobody writes the human-readable SyncLog rows), and no admin UI was ever wired to the endpoint. The reader and the model outlived a writer that never shipped.

### intel-cache-orm-model-redundant  ·  _dead-data_
- **What:** The IntelCache ORM model is dead — the intel_cache table is written and read exclusively via raw SQL, so the mapped class is a redundant, never-instantiated, never-queried definition.
- **Evidence:** app/models/enrichment.py:165 defines the IntelCache ORM model (table intel_cache). All actual access is raw SQL: app/cache/intel_cache.py:133 (INSERT INTO intel_cache) and :248 (delete), app/cache/decorators.py:150 (DELETE FROM intel_cache ... text()). Repo-wide there is no IntelCache(...) instantiation and no query(IntelCache)/db.get(IntelCache)/select(IntelCache) — only the class def and the __init__ export. (The table itself is live-but-empty on staging because no enrichment cache has been populated yet.)
- **Impact:** None functional; it is dead-code clutter that misleads readers into thinking IntelCache is accessed via the ORM and adds a maintenance/drift surface.
- **Why abandoned:** A raw-SQL cache layer (app/cache/intel_cache.py) is the real implementation; the ORM model was either the original design or kept only to satisfy the schema-drift gate, but it is never used through the ORM, leaving a decorative mapped class.

### legacy-proactive-json-router-dead  ·  _orphaned-routes_
- **What:** The entire legacy /api/proactive/* JSON API router (matches, count, offers, scorecard, send, refresh, dismiss, do-not-offer, convert, contacts, draft) is still mounted but fully superseded by the HTMX proactive router; nothing calls it.
- **Evidence:** app/routers/proactive.py (router at :26, 11 endpoints from :29) is registered at app/main.py:826/854. The live Proactive UI uses /v2/partials/proactive/* and /v2/proactive/* from app/routers/htmx/proactive.py (nav item mobile_nav.html:40 → /v2/partials/proactive; templates proactive/list.html, _match_row.html, prepare.html post there). grep across app/templates + app/static + app/**/*.py for '/api/proactive/' = 0 real refs (only a stale docstring in services/proactive_email.py:7). Router endpoint functions (list_proactive_matches, send_proactive, etc.) have no importers; only the underlying services/proactive_* stay alive (used by scheduler).
- **Impact:** None functionally (dead code), but the app publicly serves 11 duplicate authenticated endpoints that widen attack surface and mislead maintainers into thinking there are two proactive backends.
- **Why abandoned:** Classic post-migration residue: the HTMX rewrite replaced the JSON API but the old router was left included in main.py. Services are shared/alive; only the HTTP surface is dead.

### legacy-tags-rest-api-dead  ·  _orphaned-routes_
- **What:** The /api/tags/* tag REST API (list all tags, entity tags, material-card tags, tag→entities) has no consumer; tag display/edit in the UI goes through the HTMX company-tag partials instead.
- **Evidence:** app/routers/tags.py routes at :24 GET /api/tags/, :51 /{tag_id}/entities, :88 /entities/{type}/{id}, :120 /material-cards/{id}. grep across app/templates + app/static/*.js + app/**/*.py (excl the router) for '/api/tags','tags/entities','tags/material-cards' = 0. Tag UI is served by app/routers/htmx/companies/tags.py (/v2/partials/companies/{id}/tags…) and rendered by customers/_contact_tags.html, _segment_tags.html, vendors/vendor_card.html — none hit /api/tags/*.
- **Impact:** Dead code: 4 authenticated read endpoints exposing tag/entity relationships with no caller.
- **Why abandoned:** The JSON tag API predates/parallels the HTMX tag partials and was never removed after the tag UI moved to server-rendered partials. Router only touched by the tree-wide ruff/mypy chores since — no functional use.

### legacy-ai-freeform-endpoints-dead  ·  _orphaned-routes_
- **What:** A cluster of AI JSON endpoints in ai.py (draft-rfq, parse/apply-freeform-rfq, parse-freeform-offer, save-freeform-offers, company-intel, find-contacts, prospect-contacts list/save/promote/delete, parse-response/{id}) is superseded by the HTMX paste/parse and vendor find-contacts flows and is called by nothing.
- **Evidence:** app/routers/ai.py endpoints /api/ai/{draft-rfq, parse-freeform-rfq, apply-freeform-rfq, parse-freeform-offer, save-freeform-offers, company-intel, find-contacts, prospect-contacts…, parse-response/{id}} — each has 0 UI refs (grep templates+static) AND 0 internal importers (grep app/**/*.py excl ai.py & tests). Live paste/parse flow uses /v2/partials/requisitions/parse-email|parse-offer|import-save (offers.html hx-post lines); vendor contact-finding uses /v2/partials/vendors/{id}/ai/find-contacts (find_contacts_tab.html:16). Note: sibling ai.py endpoints save-parsed-offers (7 internal refs) and intake-parse (1) ARE live and excluded.
- **Impact:** Dead code: ~10 authenticated AI endpoints (some invoking Claude/enrichment) with no caller — cost/attack-surface risk and maintainer confusion.
- **Why abandoned:** Earlier freeform-intake / prospect-contact JSON endpoints left mounted after the requisition paste/parse and vendor find-contacts features were reimplemented as HTMX partials. Router last functional change 2026-06-28.

### activity-timeline-json-parallels-dead  ·  _orphaned-routes_
- **What:** The JSON timeline endpoints /api/activity/account/{id}, /api/activity/contact/{id}, and /api/activity/vendors/{id}/last-call are dead parallels of the server-rendered Activity tab / vendor card and are called by nothing.
- **Evidence:** app/routers/activity.py:321 get_account_timeline_endpoint, :356 get_contact_timeline_endpoint, :439 get_vendor_last_call. grep templates+static for 'activity/account','activity/contact','/last-call' = 0; htmx_app.js only builds '/api/activity/'+id+'/call-outcome' and '/api/activity/outreach-initiated' (not these). The account/contact activity timeline is server-rendered by the company_tab route into customers/tabs/activity_tab.html (uses the activity_row macro + preloaded context, not a JSON fetch). Underlying services get_account_timeline/get_contact_timeline (activity_service.py:861/899) are reused by other endpoints, so only these three HTTP handlers are dead.
- **Impact:** Dead code: 3 authenticated endpoints returning per-account/contact/vendor activity with no consumer.
- **Why abandoned:** Timeline JSON endpoints from the CRM call/email-tracking build; the UI landed as server-rendered tabs instead, leaving these unreferenced.

### misc-legacy-json-parallels-and-unwired-triggers  ·  _orphaned-routes_
- **What:** A tail of individually-orphaned endpoints left mounted after HTMX/feature reworks or never wired to a control: an unwired OneDrive file-picker, a duplicate-company checker superseded by entity-specific ones, the /api/error-reports alias, manual email-mining triggers, materials bulk ops, and several JSON parallels.
- **Evidence:** All verified 0 UI refs (templates+static) and non-webhook/non-OAuth: app/routers/crm/offers.py browse_onedrive (/api/onedrive/browse — 'Browse user's OneDrive files for the picker', no picker UI exists); app/routers/crm/companies.py check_company_duplicate (/api/companies/check-duplicate — forms instead use /v2/partials/customers/check-duplicate and /api/vendors/check-duplicate); app/routers/error_reports.py /api/error-reports[/{id}] POST/GET/GET/PATCH (live path is /api/trouble-tickets/* per htmx_app.js:224/237); app/routers/sources.py /api/email-mining/{scan,scan-outbound,status,compute-engagement} (mining runs via scheduler jobs/email_jobs.py using EmailMiner directly, no UI button); app/routers/materials.py /api/materials/by-mpn/{mpn}, /import-part-numbers, /merge, /materials/backfill-manufacturers; plus /api/requisitions/counts, /api/users/list, /api/admin/sync-logs, /api/changelog/{type}/{id}, PUT /api/sightings/{id}/unavailable, /api/vendor-card/add-email, /api/enrich/vendor/{card_id}. Reverse sweep confirmed no UI reference resolves to any of these and no dynamic base (other than /api/activity/) exists.
- **Impact:** Mostly dead code (attack surface + maintainer confusion); the OneDrive picker is a partially-built feature (backend browse endpoint with no picker UI).
- **Why abandoned:** Accumulated legacy/parallel endpoints from successive HTMX migrations plus admin/debug triggers whose buttons were never built (OneDrive picker) or were removed. None have callers or internal importers.

### stale-ignore-refs-to-deleted-test_browser_e2e  ·  _disabled-tests-ci_
- **What:** Two test-config files still special-case a Playwright test file that no longer exists in the repo.
- **Evidence:** pytest.ini addopts includes `--ignore=tests/test_browser_e2e.py` and scripts/ci_shard.py:38 has EXCLUDED_FILE_NAMES = {"test_browser_e2e.py"}, but `ls tests/test_browser_e2e.py` -> 'No such file or directory'. git log shows the file was last touched da3fd272 (2026-03-13 Phase 1 MVP strip-down) and was subsequently deleted; the Python e2e coverage moved to tests/e2e/. The ignore/exclusion entries were never cleaned up.
- **Impact:** None functional; a small maintenance smell that misleads a reader into thinking an active browser-e2e file is being deliberately excluded.
- **Why abandoned:** Leftover config from a deleted file — harmless dead config, not a coverage gap (there is nothing to run). Traced by confirming the file is absent while both references persist.

### redundant-dead-supplier-key-settings-fields  ·  _flags-settings_
- **What:** Three supplier API-key Settings fields exist in config.py but are never read; the connectors resolve those keys exclusively through the credential store / env, so the Settings fields are dead.
- **Evidence:** app/config.py:96/99/102 declare oemsecrets_api_key, sourcengine_api_key, element14_api_key. Zero `settings.<field>` / `getattr(settings, ...)` reads anywhere (grep returns 0 for each). The connectors instead read via credential store: app/services/connector_registry.py:234/238/242 `_cred("OEMSECRETS_API_KEY")` / `SOURCENGINE_API_KEY` / `ELEMENT14_API_KEY`, and app/search_service.py:1297-1299.
- **Impact:** Benign but misleading: the fields imply these keys flow through Settings when they do not; low-risk config drift that a future reader can misuse (setting settings.* expecting it to reach the connector).
- **Why abandoned:** When these connectors were migrated to DB-first credential resolution (get_credential/_cred reading the raw env var), the corresponding pydantic Settings fields were left behind and never removed — pure redundant/dead config with no consumer.


## Dead data layer (columns/tables never used) (6)

### dead-site-contact-owner-column-and-index  ·  _code-markers_
- **What:** site_contacts.contact_owner_id is a dead column (contact ownership was moved to site -> account owner) documented as always-NULL and 'retained to avoid a migration (Phase 1 cleanup)'; the column, its index, and its relationship were never dropped.
- **Evidence:** app/models/crm.py:307-309 ('DEPRECATED / UNUSED ... Column is retained to avoid a migration; it will always be NULL for new contacts'). Still carries a relationship (crm.py:361) and a dedicated index (crm.py:406, Index('ix_site_contacts_contact_owner_id', ...)). Two writers explicitly skip it (routers/htmx/companies/_registries.py:88, contacts.py:714). Live staging DB: SELECT count(*),count(contact_owner_id) FROM site_contacts = 17|0 (entirely NULL); index ix_site_contacts_contact_owner_id present in pg_indexes.
- **Impact:** Dead column + unused index on a live table; a decision-gated cleanup (drop vs formally keep) that never reached the user, so it never resurfaces on its own.
- **Why abandoned:** The 'Phase 1 cleanup' migration was never authored — column kept 'to avoid a migration.' Directly analogous to the tracked enrichment_credit_usage drop and companies.account_type index items, but this column+index pair is a distinct, untracked schema-drift residue (not in the 17-item report or roadmap).

### buyer-vendor-stats-no-populator  ·  _dead-data_
- **What:** The per-buyer x per-vendor performance stats table (RFQs sent, response rate, offers won, win rate, avg response hours) is documented as 'Auto-populated' but no code ever populates or reads it — a dead table and a stale docstring claim.
- **Evidence:** app/models/performance.py:267 BuyerVendorStats with docstring line 270 'Auto-populated.' (table buyer_vendor_stats, unique (user_id, vendor_card_id) + 3 indexes), created in alembic/versions/001_initial_schema.py. Repo-wide the only references are the FK-repoint list + a comment in app/services/vendor_merge_service.py:79,151 ('deliberately ABSENT here'). No writer (0 BuyerVendorStats(...) / add / insert in app, scripts, jobs), no reader, no template. Live buyer_vendor_stats=0 rows.
- **Impact:** Per-buyer/vendor relationship analytics (which buyer works best with which vendor, personal win-rate/response-time) are unavailable; the leaderboard/affinity story that would use this has no data source.
- **Why abandoned:** The 'auto-populate' job the docstring promises was never built; the table has existed since the initial schema and nothing has ever written a row. The merge-service references are defensive FK cleanup for rows that never materialize.

### vendor-quality-metrics-decorative-columns  ·  _dead-data_
- **What:** A cluster of vendor delivery-quality metric columns (on-time delivery, RMA rate, quote accuracy, lead-time accuracy) were schema-designed on the vendor snapshot and vendor card but the computation was never implemented — the columns are never written and never read.
- **Evidence:** app/models/performance.py:30-35 VendorMetricsSnapshot defines quote_accuracy, on_time_delivery, rma_rate, lead_time_accuracy. The SOLE writer app/services/vendor_scorecard.py:293-307 sets response_rate/quote_conversion/po_conversion/avg_review_rating/composite_score/etc. but NEVER these four. Repo-wide grep for each of on_time_delivery/quote_accuracy/rma_rate/lead_time_accuracy = zero references outside the model (no writer, no reader, no template). vendor_cards.rma_rate is likewise 0-ref. pos_in_window (performance.py:46) is read at vendor_scorecard.py:456 but never written (always default 0). Live: all four are 100% NULL across 68,382 vendor_metrics_snapshot rows and 1,169 vendor_cards.
- **Impact:** Vendor scorecards can never surface on-time-delivery %, RMA rate, quote accuracy, or lead-time accuracy (and always show 0 POs-in-window), so the vendor performance picture is permanently partial versus what the schema advertises.
- **Why abandoned:** The scorecard schema was laid out for a fuller vendor-quality model (delivery performance, RMA tracking, quote accuracy, lead-time accuracy) but the aggregation logic to compute them was never written; the snapshot writer only fills the subset it knows how to compute. These are decorative columns left from an over-provisioned design.

### tag-threshold-config-seed-missing-on-live  ·  _dead-data_
- **What:** The tag-visibility threshold config table is seeded by migrations but is empty on the deployed DB, which makes the entity-tag two-gate visibility system force every non-segment tag to hidden.
- **Evidence:** Seed rows are inserted unconditionally by alembic/versions/042_add_tagging_tables.py:152,165 (op.bulk_insert) and again by 046_fix_threshold_entity_types.py:33; live alembic head is 189 (well past both). Consumer app/services/tagging.py:261-278: it loads thresholds from TagThresholdConfig and, when a tag_type has no matching config row, sets et.is_visible=False (line 277). Live DB: tag_threshold_config=0 rows, and as a direct consequence all 21 entity_tags rows have is_visible=false (verified via GROUP BY). Same seed-loss pattern also hit knowledge_config (migration 064 seeds daily_question_cap, live table=0 rows).
- **Impact:** Brand/commodity tags on companies, vendors and sites can NEVER become visible (the gate always evaluates to hidden with an empty config), so the entity-tag visibility feature is effectively off in production even though the tagging pipeline (144k material_tags) runs.
- **Why abandoned:** Two independently migration-seeded config tables (tag_threshold_config, knowledge_config) are both empty on the live DB despite the seed INSERTs running on upgrade — the deployed database was evidently built from a schema baseline / stamp that skipped these data-seed steps, so the config that gates the feature is silently absent. This is a data-layer residue, not a code bug, but it disables shipped behaviour.

### enrichment-runs-legacy-orphan-table  ·  _dead-data_
- **What:** The legacy autonomous-enrichment orchestrator's run-state table survives with orphaned rows after the orchestrator was deleted — nothing writes or reads it now; it awaits a drop decision that was never routed (the exact twin of the tracked enrichment_credit_usage item).
- **Evidence:** app/models/enrichment_run.py:1-17 docstring states the orchestrator (scripts/enrich_orchestrator.py + enrichment-entrypoint.sh) 'was removed... so nothing writes these rows anymore' and the model is 'retained purely to keep Base.metadata in sync... Dropping the table is a separate, riskier decision... deliberately out of scope'. Repo-wide the only reference is the __init__.py:58 export (zero writers/readers in app/scripts/jobs). Live enrichment_runs=28 orphaned legacy rows (the pg_stat estimate reads 0, direct count reads 28).
- **Impact:** None functional (dead legacy data); it is schema-drift residue carrying stale rows that should be surfaced as a one-line drop/keep decision alongside enrichment_credit_usage.
- **Why abandoned:** The paced app.services.enrichment_worker superseded the phase-based orchestrator; the orchestrator code was removed but the table + 28 stale run rows were left behind and the drop was punted to 'a separate decision' that never reached a backlog. It is the same decision-gated-drop category as the tracked enrichment_credit_usage, but this second sibling is untracked.

### dead-schema-columns-enums-decide-then-drop  ·  _memory-parked_
- **What:** A scoped 'decide-then-drop' cleanup for dead DB columns and dead enum members was planned but never routed to the user or executed.
- **Evidence:** Dead columns still present on main: app/models/prospect_account.py:59 ProspectAccount.import_priority, app/models/crm.py:101 Company.import_priority, app/models/excess.py:115 ExcessLineItem.demand_match_count (+ its index). Dead enum members flagged in the inventories (QualityPlanStatus IN_REVIEW, ExcessListStatus.ACTIVE/BIDDING, ExcessOfferStatus.EXPIRED, RiskFlagType, SpecCodeSource.MANUAL/CSV_IMPORT, etc.). Scoped in docs/superpowers/2026-07-04-remaining-work-inventory.md §B, docs/superpowers/2026-07-05-unfinished-work-inventory.md ('DEAD SCHEMA — one decide-then-drop migration'), and memory project_audit_remediation_waves_2026_07_04 ('QUEUED — Wave 3 … dead model columns … dead-column drops need a migration + care … get a nod before dropping columns').
- **Impact:** No direct user-facing impact (internal cleanup), but the dead columns/enums persist as schema noise that the drift gate and any future migration-from-scratch DB rebuild must keep carrying, and the routed keep-or-drop decision never reached the owner.
- **Why abandoned:** The audit-remediation Wave-3 deferred the column drops into a single consolidated migration guarded by the drift-gate never-drop rules, explicitly requiring a user 'nod' on a DB awaiting SFDC import; that decision was never surfaced. Distinct from the already-tracked single-table enrichment_credit_usage drop — this is the broader dead-column/dead-enum set. Not in the master roadmap or the 17-item report.


## Disabled / silently-skipped tests (3)

### zombie-performance-api-tests-masked-by-mvp-skip  ·  _disabled-tests-ci_
- **What:** The buyer/sales performance scoring HTTP layer (Avail Score, Multiplier Score, bonus winners) — its API endpoints were deleted in March but the scores are still computed nightly and are now unviewable, while the endpoint tests that would catch this are silently skipped by a misleading gate.
- **Evidence:** tests/test_avail_score.py:689 (TestAvailScoreAPI) and tests/test_multiplier_score.py:673 (TestMultiplierAPI) assert GET /api/performance/avail-scores and /api/performance/multiplier-scores return 200. But app/routers/performance.py (274 lines, the router serving those paths) was DELETED in commit 5ea26ba0 (2026-03-14, 'remove dead ... performance code'); a full-tree grep for 'avail-scores'/'multiplier-scores'/'bonus-winners' in app/ returns nothing (no router, no template, no JS consumer). The tests are kept green only by @pytest.mark.skipif(os.environ.get('MVP_MODE','true').lower()=='true', reason='post-MVP Performance module (MVP_MODE-gated by design)'). tests/conftest.py never sets MVP_MODE, so the raw default 'true' makes them skip on EVERY CI run; no CI or nightly step sets MVP_MODE for pytest. Meanwhile staging .env:106 has MVP_MODE=false. The compute services survive and still run monthly via app/jobs/offers_jobs.py:136,144 (_job_performance_tracking writing AvailScoreSnapshot), yet grep of app/templates + app/routers for AvailScoreSnapshot/MultiplierScore returns nothing — scores are written but unread. The skip was made 'explicit' in b4f61927 (2026-07-05, item 3.3).
- **Impact:** Buyer/sales Avail-Score, Multiplier-Score and bonus-winner data (money/comp-adjacent) is computed and stored every month but no page or API can display it; the two test classes that would fail loudly on the missing endpoints are permanently skipped behind a false 'by design' justification, so no signal ever reaches the team.
- **Why abandoned:** The endpoints were removed in 5ea26ba0 (2026-03-14), not MVP-gated, so the skip reason 'MVP_MODE-gated by design' is stale/false: if the gate were real, MVP_MODE=false on staging would make them live, but they don't exist at all. If un-skipped the tests 404. Rather than delete the zombie tests (dead endpoint) or rebuild the surface (scores are still computed nightly with no way to view them), the July-5 hardening pass rationalized them into an always-skip. I traced: git log -S on the paths -> router file deleted; grep for consumers -> none; scheduler -> compute still active -> concluding the feature is half-alive (compute on, surface + tests off) and the disabled tests hide it.

### openapi-contract-validation-silently-skipped-schemathesis-4x  ·  _disabled-tests-ci_
- **What:** The OpenAPI contract test — the only check that API responses actually conform to their declared schemas — silently skips on every run because it was written for the schemathesis 3.x API but the pinned dependency is 4.x.
- **Evidence:** tests/test_contract.py:53-60: test_contract_health branches on hasattr(schemathesis,'from_asgi') / hasattr(schemathesis,'from_dict') and otherwise hits pytest.skip('schemathesis version lacks from_asgi/from_dict'). schemathesis IS installed in CI (requirements-dev.in:15 schemathesis>=4.22.4, requirements-dev.txt:277 schemathesis==4.22.4; ci.yml:91 installs requirements-dev.txt), so HAS_SCHEMATHESIS is True and the 'not installed' skip is NOT the cause. I installed schemathesis==4.22.4 in isolation and confirmed: top-level from_asgi=False, from_dict=False (the 4.x API relocated these to schemathesis.openapi.from_asgi=True). So both hasattr checks fail -> the test skips every run. The sibling test_openapi_schema_is_valid (line 24) still runs but only asserts /openapi.json parses, not that any response matches its response_model. The master roadmap (docs/superpowers/2026-07-16-master-execution-roadmap.md:68) plans the docs-lockdown around this test and ASSUMES it runs ('it fetches /openapi.json via the ASGI app + schemathesis'), a stale assumption.
- **Impact:** API response/schema drift (a 200 whose body no longer matches its declared shape) is completely unguarded; the safety net reads green while asserting nothing. This is separate from the tracked docs-lockdown task and actually invalidates that task's premise.
- **Why abandoned:** A schemathesis major-version bump (3.x -> 4.22.4) removed the top-level from_asgi/from_dict loaders the test dispatches on, so the file's stated purpose ('Validates that all API responses match their declared response_model schemas') has silently produced zero contract coverage. A skip counts as a pass, so CI stayed green and nobody noticed the loader path went dead. Verified by inspecting the actual installed 4.22.4 module surface rather than by memory.

### migration-170-pg-trgm-index-test-never-runs  ·  _disabled-tests-ci_
- **What:** The test that verifies migration 170 actually created the prospecting pg_trgm GIN indexes (warm-intro search) is gated on an env var that is set nowhere, so it never executes in any environment.
- **Evidence:** tests/test_prospect_buyer_ready_persistence.py:126 test_warm_intro_trgm_indexes_exist_after_migration is skipif(not PROSPECTING_TRGM_TEST_DB_URL). A repo-wide grep (excluding .venv) finds PROSPECTING_TRGM_TEST_DB_URL only inside that one test file — it is set in no CI workflow, no script, no .env, no docs. It uses a DIFFERENT env var than the working requires_postgres path (PG_TEST_DSN, which IS set by the postgres-paths CI job), and specifically wants a DB 'migrated to head (170)' to assert ix_sightings_vendor_email_trgm / ix_site_contacts_email_trgm (GIN) / ix_prospect_accounts_buyer_ready_score exist. Severity is bounded: all three indexes are declared in ORM __table_args__ (app/models/crm.py:412, app/models/sourcing.py:335, app/models/prospect_account.py:120), so the schema-drift gate (alembic upgrade head + check_schema_matches_models.py) and the create_all-based postgres-paths job backstop their presence/GIN build.
- **Impact:** Low direct risk (backstopped by the drift gate), but it is a dead migration-verification test giving false comfort; a future migration that dropped/altered these prospecting search indexes without touching the model would slip past this specific check because it can never fire.
- **Why abandoned:** The test was written with a bespoke activation knob (PROSPECTING_TRGM_TEST_DB_URL) that was never plumbed into CI when the postgres-paths harness standardized on PG_TEST_DSN, so it is permanently skipped. Its unique value (proving migration 170 itself, not create_all, builds the GIN indexes) never runs. I confirmed the knob exists nowhere via grep and confirmed the drift-gate overlap by locating the indexes in the models.


## Config/flag present but unwired (1)

### dependabot-dependencies-label-never-created  ·  _github-prs-issues_
- **What:** Dependabot is configured to tag every dependency-update PR with a `dependencies` label, but that label was never created in the repo — so the tag is silently applied to nothing and Dependabot prints a config error on every PR it opens.
- **Evidence:** .github/dependabot.yml declares `labels: [dependencies]` on all three update groups (lines 10, 49, 77). The repo's label set (gh label list) contains bug/documentation/duplicate/enhancement/good first issue/help wanted/invalid/question/wontfix/trouble-report/ci/database/tech-debt/flaky-test — NO `dependencies` label. Every dependabot PR carries the comment: 'The following labels could not be found: `dependencies`. Please create it before Dependabot can add it to a pull request. Please fix the above issues or remove invalid values from dependabot.yml.' — verified on closed PRs #711, #712, #713, #724, #725. Across all 18 dependabot PRs (open+closed+merged), 0 carry the `dependencies` label. Not in the 17-item report, roadmap, Resell plan, migration-189 residuals, or any known-exclusion.
- **Impact:** Low. No security/production bumps were dropped — dependency updates still merge. Impact is: (1) a recurring config-error comment on every dependabot PR (noise), and (2) any label-based triage/filter/auto-merge/notification rule keyed on `dependencies` matches zero dependabot PRs, so such automation is silently inert.
- **Why abandoned:** Traced by reviewing every closed-unmerged PR in the lane: the 5 closed dependabot PRs (#711/#712/#713/#724/#725) each surface the identical 'labels could not be found: dependencies' warning as their first bot comment, and the same warning appears on the merged ones too. I then confirmed via `gh label list` that the `dependencies` label genuinely does not exist and via the PR label data that it has been applied to 0/18 dependabot PRs — i.e. the label directive has never once worked since dependabot.yml was written. It is self-perpetuating (fires on every future dependabot PR) and invisible to normal review because the underlying version bumps still merge (rebased duplicates #715/#717/#720/#726/#732/#733 landed), so nobody notices the labeling half silently no-ops. Fix is a one-liner: create the `dependencies` label (or drop the three `labels:` blocks from dependabot.yml).


## Stale claim (reads done, isn't) (1)

### dead-behavior-toggle-env-vars-footgun  ·  _flags-settings_
- **What:** Several feature/behavior toggles that read as active are silently ignored: the installer bakes three 'Behavior' knobs into every provisioned .env, and the staging .env sets four feature flags — none of which any code reads (config uses extra='ignore').
- **Evidence:** scripts/bootstrap-server.sh:180-182 writes OUTREACH_COOLDOWN_DAYS=30, POLL_INTERVAL_MINUTES=5, AUTO_SIGHTING_CONFIDENCE=0.7 into the generated .env; grep finds no consumer for any of the three (POLL_INTERVAL_MINUTES only appears in bootstrap + docs referring to the unrelated EIGHT_BY_EIGHT_POLL_INTERVAL_MINUTES). Staging .env:55/71/121/124 set DEEP_EMAIL_MINING_ENABLED, DEEP_ENRICHMENT_ENABLED, MATERIAL_ENRICHMENT_ENABLED, AVAIL_OPP_TABLE_V2 =true; none is a config.py field and none is read via os.getenv anywhere — the underlying deep-enrichment (trigger_deep_enrichment_bg) and material_enrichment_service run ungated by these vars, and AVAIL_OPP_TABLE_V2 has zero matches. .env:61 APOLLO_API_KEY is a stale credential for a source that startup.py:1584 explicitly prunes as retired.
- **Impact:** Operator-facing footgun: flipping DEEP_ENRICHMENT_ENABLED, MATERIAL_ENRICHMENT_ENABLED, AUTO_SIGHTING_CONFIDENCE, OUTREACH_COOLDOWN_DAYS, etc. changes nothing, giving a false sense of control over enrichment depth, outreach cadence, and sighting auto-accept thresholds.
- **Why abandoned:** These are config-hygiene residue: toggles for features that either run unconditionally, were retired (Apollo), or never existed (AVAIL_OPP_TABLE_V2). They were left in the installer template and the live .env where they read as operative switches. Distinct from the tracked stale-CLAUDE.md-keys item (that is documentation; these are the version-controlled installer and the live .env).
