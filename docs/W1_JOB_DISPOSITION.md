# W1.0 — Scheduled-job disposition table

Session 1, 2026-08-04. All 59 in-app scheduler jobs (app/jobs/*.py)
classified against the spec §3 kernel list, §6 specifics, and §5/§8
park lists. Every numeric claim was spot-checked against the restored
production copy (10/10 queries confirmed by an independent verify
pass). This table drives W1.1–W1.4; each disposition lands as its own
commit. Deletes are git-restorable; parks use EXISTING flags only.

**Result: keep 7 · park 12 · delete 40** (reconciled to spec v1.1, 2026-08-04: 8x8 CDR poll moved delete → park per §0.3/§6 Data Capture Initiative).

v1.1 reconciliation notes: **eight_by_eight_poll** parks behind its existing
`eight_by_eight_enabled` flag (already False), comeback = Data Capture
Initiative (kernel E2E green in production + launch declared). The other
call/email-capture deletes stand — **teams_call_records_sync** and
**contacts_sync** are not named in §6's initiative (Outlook email + 8x8
calls) and have zero-yield evidence; git history is their path back into
the initiative rebuild, which §6 describes as "rebuilt properly" anyway.

## Kernel mapping (spec §3 — scheduler must equal this exactly)

| §3 kernel entry | Job(s) |
|---|---|
| inbox reply scan | inbox_scan + components: token_refresh, batch_results (AI-parse half), scan_sent_folders (reply-matching reconciliation) |
| approval outbox email | approval_outbox_drain |
| cadence/follow-up clocks | cadence_materialize |
| worker liveness | worker_liveness_check |
| db-backup, nightly E2E | not app-scheduler jobs (container + host cron) |

## Flip-able calls for Packet 1 (executed as recommended; one click to reverse)

1. **bid_due_alerts → DELETE** (recommended): not on the §3 list; 2 tasks ever created. Flip = keep it as a follow-up clock.
2. **auto_attribute_activities → DELETE** (recommended): AI attribution of activity rows; 9,772 of 12,851 unattributed — not keeping up; single-user attributes at log time. Flip = park behind AI flag.
3. **auto_dedup → DELETE** (recommended): AI-confirmed background merges of CRM masters; manual merge path stays. Flip = park behind AI flag.
4. **inbox_scan sub-ops**: the job stays (kernel), but its EmailMiner side-harvest (source of 1,312 vendor_cards) is §6 email_mining machinery — W1 trims the sub-ops, keeps the scan.

## Full table

| Job | Disposition | Spec | Evidence |
|---|---|---|---|
| approval_outbox_drain | KEEP | §3 kernel: approval outbox email | Every 60s drains approval_outbox rows (sent_at IS NULL, fail_count < 5) → Graph email / in-app Notification, per-row commit + dead-letter cap. DB: 6 rows, all 6 sent — working. |
| batch_results | KEEP | §6 reply-scan pipeline (AI parse half); §3 kernel | Reads pending_batches (PendingBatch PROCESSING) → applies Anthropic inbox-parse results to VendorResponses. DB: 2,074 pending_batches, ALL batch_type=inbox_parse status=completed, latest submitted today 12:50 — active pipeline;… |
| cadence_materialize | KEEP | §3 kernel list (cadence/follow-up clocks) + §5.4 keep (cadence clocks) | Reads ActivityLog and recomputes the cadence-clock columns on every Company via materialize_all_clocks (nightly 4AM); 13 companies in prod DB. Named on the kernel job list. |
| inbox_scan | KEEP | §3 kernel step 4; §6 'KEEP the reply-scan pipeline' | Polls each connected user's inbox (poll_inbox) → VendorResponse/ActivityLog rows feeding Responses. DB: sync_state 'inbox' last sync today 12:50. Wave-1 note: its EmailMiner sub-ops (mine_contacts/outbound_scan — source of 1,31… |
| scan_sent_folders | KEEP | §3 kernel (reply-scan pipeline component + cadence/follow-up clock feed); §6 | Delta-scans SentItems, reconciles send-time ActivityLog rows and stamps graph_message_id/graph_conversation_id onto Contact rows so poll_inbox Tier-1 conversation matching works (email_service.py:337 'scan_sent_folder will reco… |
| token_refresh | KEEP | §3 kernel (component of inbox reply scan + approval outbox email) | Reads users.refresh_token/token_expires_at, writes refreshed Graph access_token + m365_error_reason; both kernel Graph jobs (inbox scan, outbox email send) depend on valid tokens. DB: 1 user with refresh_token, scans running to… |
| worker_liveness_check | KEEP | §3 kernel background jobs — 'worker liveness' | Reads last_heartbeat/is_running/circuit_breaker_open from the four worker-status singletons (ICS, NetComponents, TBF, Enrichment) and alerts via logs+Sentry+Teams with Redis-debounce, writing nothing to business tables (worker_… |
| account_sweep | PARK | §5.4 park: multi-rep pool governance (claim caps, assign modal, manager digests) | Daily 1AM: clears the owner of dormant owned accounts and parks them into the prospecting pool with cooldown+notify (prospect_reclamation.job_account_sweep) — rep-book reclamation over 13 companies. Already flag-gated (PROSPECT… Comeback: team exists (§5.4) |
| auto_surface_reactivation | PARK | §5.4 park: multi-rep pool governance; prospecting becomes manual-intake CRM lens | Daily 2AM: surfaces unassigned past-customer Companies as reactivation prospects into the pool (prospect_reclamation.job_auto_surface_reactivation) — automated pool feeding, replaced by §5.4 manual prospect intake. Existing fla… Comeback: team exists (§5.4) |
| buyplan_nudge | PARK | §5.4 park (org-scale coordination: multi-rep governance/manager digests) + §8 'org-scale CRM trimmings' | Reads awaiting_po/pending_verify BuyPlanLines past hour thresholds, sends buyer/ops notifications and stamps last_nudge_at. DB: 2 lines ever nudged; single operator = the job nudges the owner about the owner's own lines. Comeback: Team exists (second buyer/ops user, per §5.4) |
| calendar_scan | PARK | §5.4/§8 park: org-scale CRM machinery | Daily Graph /calendarView scan → ActivityLog meeting rows (external_id 'calendar-…'). DB: 39 meeting rows, latest today 06:00 cron — live yield, so park not delete; already gated by existing activity_tracking_enabled flag. Not … Comeback: team exists (§5.4/§8 org-scale CRM trimmings park) |
| ownership_sweep | PARK | §5.4 park: multi-rep pool governance / org-scale CRM trimmings (§8 park list) | WARNINGS-ONLY: emails the owner of each account nearing account_sweep_inactivity_days (run_ownership_sweep docstring); pure multi-rep governance over a 13-company book. Already flag-gated (OWNERSHIP_SWEEP_ENABLED + activity_tra… Comeback: team exists (§5.4) |
| performance_tracking | PARK | §5.4 park (Activity Scorecard / org-scale machinery) + §8 'org-scale CRM trimmings' | Every 12h computes vendor scorecards, buyer leaderboard, Avail/Multiplier/Unified score snapshots (plus prev-month grace recompute). DB: 99,546 vendor_metrics_snapshot rows — the heaviest writer in this batch — versus 18/27/27/… Comeback: Team exists (§5.4) |
| proactive_matching | PARK | §4 + §8 (Proactive workspace parked behind existing flag); tension: §4/§5.4 keep 'matching engine + badge live inside Deals' while §11 wants scheduler == kernel exactly — parent must resolve whether the scan re-enables when the Wave-4 Deals badge lands | Scans new offers/sightings against CPH + archived reqs, writes ProactiveMatch rows and expires old ones. Already flag-gated (proactive_matching_enabled; system_config=true today, last scan 2026-08-04 06:00). DB: 18 matches ever… Comeback: Proactive workspace revival / Deals-merge badge wiring needs matches (§4: engine + badge live inside Deals, Wave 4) |
| proactive_teams_push | PARK | §8 park (Proactive workspace, behind existing flag) | Pushes not-yet-pushed NEW ProactiveMatch rows to Teams as an Adaptive Card digest (watermark-idempotent). Already flag-gated off: proactive_teams_push_enabled defaults False and has no system_config override row, so the job is … Comeback: Proactive workspace revival (§8) — flag flips on with it |
| quality_score_activities | PARK | §5.4 park list — Activity Scorecard (its only consumer) | Every 15min sends unscored non-email ActivityLog batches (50) to Claude Haiku and writes quality_score/quality_classification/clean_summary (app/services/activity_quality_service.py:217). DB: 7,245 of 12,851 activities scored, … Comeback: team exists (§5.4) |
| recompute_buyer_scores | PARK | §5.3 explicit park: 'the buyer-intelligence layer (BuyerScore, ranked suggestions, nudge, auto My-Day tasks, nightly recompute jobs)' | Nightly recomputes every buyer's BuyerScore scorecard (walks every VendorCard with an ExcessOffer/ExcessOutreach, upserts buyer_scores) as backstop for missed on-win/on-send hooks. DB: 6 buyer_scores rows; only consumer (ranked… Comeback: Second trader user exists (§5.3) |
| site_ownership_sweep | PARK | §5.4 park: multi-rep pool governance / org-scale CRM trimmings (§8 park list) | Clears CustomerSite.owner_id after 30 days inactivity and logs warnings from day 23 (run_site_ownership_sweep) — rep-territory reclamation, meaningless with one operator. Same existing flag gate as ownership_sweep. Comeback: team exists (§5.4) |
| ai_tagging | DELETE | §6 tagging suite — 'AI tagging runs only on-demand when AI keys are on' | Reads 500 brand-untagged material_cards per run, calls Claude Haiku, writes material_tags source='ai_classified' (app/services/tagging_ai.py via _apply_ai_results). DB: 123,840 ai_classified tags prove yield, but it is paid-API… Comeback: on-demand only, when AI keys are on (§6) |
| auto_attribute_activities | DELETE | §3 kernel list / §8 DELETE mechanics (no named park trigger in spec) | Reads unmatched non-dismissed activity_log rows (batch 200), rule-matches then calls Claude to attribute to company/vendor, auto-dismisses >30d (app/services/auto_attribution_service.py:28). DB: 9,772 of 12,851 activities sit u… |
| auto_dedup | DELETE | §3 kernel list / §8 DELETE mechanics (no named park trigger in spec) | Fuzzy-matches company/vendor name pairs then calls Claude to confirm before merging rows (app/services/auto_dedup_service.py:44). AI-cost background merges of CRM masters are exactly the unattended machinery §3 excludes; the ma… |
| batch_parse_signatures | DELETE | §8 delete (dead); §6 AI on-demand only; §5.4 contact-intelligence cut | Submits low-confidence signatures to Claude Batch, caching in email_signature_extracts. DB: email_signature_extracts has 0 rows EVER — the pipeline has never produced or had anything to process. |
| bid_due_alerts | DELETE | §3 kernel-only (§11 strict); FLIP-ABLE in Packet 1 | Not on the §3 job list. DB: 2 bid-due tasks ever created, 3 reqs with parseable deadlines — near-zero yield; git restores. |
| cache_cleanup | DELETE | §3 kernel list (not on it) / §11 'scheduler job list == kernel list exactly' | Deletes expired rows from the intel_cache PG table in 1000-row batches (app/cache/intel_cache.py:248). DB: intel_cache has 0 rows total (Redis is the primary store; get_cached already TTL-checks at read time) — the daily job ha… |
| cleanup_usage_log | DELETE | §3 kernel list / §11 | Monthly delete of api_usage_log rows older than 90 days (health_jobs.py:48). DB: 112,082 rows, latest 2026-08-04 — once the health/connector pollers stop the table stops growing, and the trim becomes a one-line on-demand command. |
| contact_dedup | DELETE | §3 kernel list / §8 DELETE mechanics | Nightly merge of site_contacts sharing (customer_site_id, lower(email)) through merge_contacts so child rows repoint (maintenance_jobs.py:139). DB: 0 duplicate (site,email) groups exist in today's prod copy — the backlog is cle… |
| contact_scoring | DELETE | §5.4 cut: contact-intelligence layer (computed, displayed nowhere); Wave 2 delete list (§10) | compute_all_contact_scores writes VendorContact.relationship_score from ActivityLog windows; DB has only 6 vendor_contacts (all scored) and the score surfaces only as a list sort key — the exact computed-displayed-nowhere layer… |
| contact_status_compute | DELETE | §5.4 cut: contact-intelligence layer (computed, displayed nowhere); §3 kernel exclusivity | Writes SiteContact.contact_status (active/quiet/inactive) from ActivityLog recency; zero template reads it (grep of app/templates). DB: 17 site_contacts — 12 'new', 4 NULL, 1 'active' — the job has effectively moved one row ever. |
| contacts_sync | DELETE | §3 kernel-only; §8 delete (duplicated/zero-yield) | Outlook /me/contacts/delta → VendorCards. DB: 0 of 1,332 vendor_cards have source='outlook_contacts' (email_mining produced 1,312); no 'contacts_sync' sync_state delta row ever persisted; users.last_contacts_sync stale since 20… |
| discover_prospects | DELETE | §6 Explorium discovery machine (6 monthly jobs): DELETE | Runs run_explorium_discovery_batch per segment rotation, writes DiscoveryBatch(source='explorium') + prospect_accounts. DB: 5 monthly explorium batches (2026-04-01..08-01) produced 5 prospect_accounts total — all still status='… |
| eight_by_eight_poll | PARK | §6 v1.1: 8x8 CDR polling PARKED, not deleted (§0.3 Data Capture Initiative) | Polls 8x8 CDR API every 30min, writes matched calls to activity_log. DB: 0 activity_log rows with details source='8x8_cdr' EVER, while the system_config '8x8_last_poll' watermark advanced to 2026-08-04T12:49Z — needs an integration pass, not a restart. Parks behind existing eight_by_eight_enabled flag (already False). Comeback: Data Capture Initiative — kernel E2E green in production + launch declared |
| email_health_update | DELETE | §3 kernel exclusivity + §6 email pipelines (keep = reply-scan only) | batch_update_email_health writes VendorCard.email_health_score; DB: 0 of 1,332 vendor_cards have a score — zero rows ever written by this daily job. Sole reader (sightings.py responsiveness rank) falls back to engagement_score … |
| email_reverification | DELETE | §8 DELETE (dead code); §3 kernel exclusivity | run_email_reverification is a stub: logs 'Hunter connector removed, no provider configured' and returns {status: no_provider, processed: 0} — reads nothing, writes nothing, every quarterly run is a no-op. |
| enrich_pool | DELETE | §6 Explorium discovery machine (6 monthly jobs): DELETE | Monthly (2nd, 2AM) signals/similar-customers/AI-writeup enrichment of pool prospects (prospect_scheduler.job_enrich_pool); enrichment arm of the deleted machine — 5-row pool, zero actioned. |
| expire_and_resurface | DELETE | §6 Explorium discovery machine (6 monthly jobs): DELETE | Monthly (last day, 9PM) expire-stale/resurface-refreshed pass over the pool (prospect_scheduler.job_expire_and_resurface); lifecycle arm of the deleted machine. |
| expire_resell_lists | DELETE | §3 kernel list (absent) + §6 'everything else parked or deleted'; §5.3 also stops the resell→Sighting mirror maintenance, which is half this job's effect; no existing flag to park behind | Flips open/collecting ExcessLists past close_at to expired (or bid_out if partially awarded) and retires their Sighting mirror. DB: 10 lists ever, 3 with close_at set, 3 expired — the ONLY delete in this batch with live yield; … |
| expire_strategic_vendors | DELETE | §3 kernel list (absent) + §8 DELETE (dead/zero-yield, same class as §6's two zero-yield tagging jobs) | Expires strategic_vendors rows past their 39-day TTL via strategic_vendor_service.expire_stale. DB: strategic_vendors sequence never called (is_called=false) — zero rows have EVER existed. |
| find_contacts | DELETE | §6 Explorium discovery machine (6 monthly jobs): DELETE | Monthly (3rd, 2AM) procurement-contact enrichment for pool prospects (prospect_scheduler.job_find_contacts); contacts arm of the deleted machine. |
| flag_stale_offers | DELETE | §3 kernel list (absent) + §5.1 derived-not-stored precedent (pipeline status becomes derived; staleness = created_at < now-14d is the same class) | Sets Offer.is_stale=true on ACTIVE offers >14d; docstring says display-only metadata, never filtered on. DB: 832 of 1,520 offers flagged — the job does write, but readers are display chips (offers tab/sightings table) that can … |
| health_deep | DELETE | §3 kernel list / §6 job shrink; §5.5 | Every 2h runs full functional tests against the same active ApiSource set (health_monitor run_health_checks('deep')) — burns real calls where keys exist; same keyless-probe evidence as health_ping (2,190 calls_this_month across… Comeback: connector keys go on (§5.1) |
| health_ping | DELETE | §3 kernel list / §6 job shrink; §5.5 keys-off shown honestly, not polled | Every 15min runs lightweight checks against all active ApiSource rows and writes results/error counters (app/services/health_monitor.py:333). DB: 17 active sources being probed keyless, 8 accumulating 24h errors, api_usage_log … Comeback: connector keys go on (§5.1 — fan-out stays code-complete; git restores the poller) |
| integrity_check | DELETE | §3 kernel list / §11; §4 demotes Materials to a lookup | Checks requirement/sighting/offer ↔ material_card linkage, dangling FKs, duplicate cards, and self-heals orphans every 6h (app/services/integrity_service.py:235); the same report already serves the on-demand /api/admin/integrit… |
| internal_confidence_boost | DELETE | §6 tagging suite — 'the two zero-yield jobs are deleted' | Reads material_tags source='ai_classified' + card/sighting manufacturer agreement, upgrades confidence to 0.90 writing source='ai_confirmed_internal' (app/services/enrichment.py:410). DB: 0 material_tags rows with source='ai_co… |
| knowledge_expire_stale | DELETE | §8 DELETE list — 'Dashboard + Knowledge pages' | Only counts and LOGS expired knowledge_entries daily at 3AM — its own docstring says expiry is applied at query time, not by this job (knowledge_jobs.py:32). DB: 6,360 knowledge_entries; with the Knowledge pages deleted in Wave… |
| po_verification | DELETE | §3 kernel list (absent) + §2 (gate is the interactive verify_po approver action, not this job) | Reads active BuyPlans with pending_verify lines and scans the buyer's Outlook sent folder via Graph for the PO number — detection-only, explicitly never verifies (buyplan_po.py: 'DETECTION ONLY'). DB: 0 lines currently pending_… |
| poll_signature_batch | DELETE | §8 delete (dead); §6 AI on-demand only | Poller for the same signature batch pipeline (process_signature_batch_results). DB: email_signature_extracts 0 rows ever — polls an empty pipeline every 5 min. |
| pool_health_report | DELETE | §6 Explorium discovery machine (6 monthly jobs): DELETE | Monthly (1st, 8AM) stats logging over the Explorium-fed prospect pool (prospect_scheduler.job_pool_health_report) — reporting arm of the machine the spec deletes; pool holds only 5 prospect_accounts. |
| prefix_backfill | DELETE | §6 tagging suite — 'prefix/spec jobs become on-demand management commands' | Reads untagged material_cards, writes brand/commodity tags via prefix lookup table, source='prefix_lookup' (app/services/tagging_backfill.py:112). DB: 71,145 prefix_lookup tags exist but 548,867/743,865 cards remain untagged — … Comeback: on-demand management command (run manually when a card import lands) |
| proactive_offer_expiry | DELETE | §3 kernel list (absent) + §8 DELETE (dead — parked Proactive workspace leaves no producer of 'sent' ProactiveOffers) | Updates ProactiveOffer status sent→expired after 14d. DB: 6 proactive offers ever created (sequence), 0 rows exist now; with the Proactive workspace parked nothing can create new 'sent' offers, so the sweep is dead by consequence. |
| refresh_scores | DELETE | §6 Explorium discovery machine (6 monthly jobs): DELETE | Monthly (15th, 2AM) re-score of all suggested prospects (prospect_scheduler.job_refresh_scores); scoring arm of the deleted machine — all 5 prospects still 'suggested'. |
| reset_connector_errors | DELETE | §3 kernel list / §6 job shrink; §5.5 connectors show keys-off state statically | Zeroes ApiSource.error_count_24h in 50-row batches nightly (maintenance_jobs.py:114). DB: 8 of 29 api_sources carry 24h errors from keyless probing — counter churn on connectors that are off; §5.1's connector comeback (keys go … |
| reset_monthly_usage | DELETE | §3 kernel list / §11 | Zeroes ApiSource.calls_this_month on the 1st (health_jobs.py:69). DB: 2,190 calls_this_month total, all from keyless probing — a quota counter for paid connectors that are off; returns with the connector comeback. Comeback: connector keys go on (§5.1) |
| sighting_mining | DELETE | §6 tagging suite — 'the two zero-yield jobs are deleted' | Reads Sighting.manufacturer for untagged cards, writes brand tags source='sighting_consensus'/'sighting_single' (app/services/tagging_backfill.py:202). DB: 0 material_tags rows with either source ever — zero yield after months … |
| spec_enrichment | DELETE | §6 tagging suite — 'prefix/spec jobs become on-demand management commands' | Reads status-gated (verified/web_sourced/oem_sourced) enriched cards, writes specs_structured JSONB + material_spec_facets via record_spec (app/services/spec_enrichment_service.py). DB: 62,769 facet rows / 22,374 cards with str… Comeback: on-demand management command |
| stock_autocomplete | DELETE | §3 kernel list (absent) + §8 DELETE (dead/zero-yield) | Reads BuyPlan where is_stock_sale AND active >1h past approval, calls _complete_plan as a 'safety net'. DB: 0 stock-sale buy plans EVER (0 of 10 buy_plans_v3) — the job has never had a subject. |
| sweep_stale_sending_outreach | DELETE | §3 kernel list (absent) + §8 DELETE (zero-yield) | Flips ExcessOutreach rows stuck in 'sending' past a staleness threshold to 'interrupted' (durability backstop, never resends). DB: ~7 outreach rows ever (sequence=7), 4 remain, 0 ever interrupted — the failure mode it guards ha… |
| teams_call_records_sync | DELETE | §6 (8x8 CDR cut analog: zero call records ever; click-to-call + outcome chips capture the workflow); §3 kernel-only | Polls Graph /me/callRecords every 6h → log_call_activity with external_id 'teams-call-{id}'. DB: 0 activity_log rows with external_id LIKE 'teams-call-%' EVER, despite the watermark advancing (last poll today 09:20); the BUYER/… |
| warn_strategic_expiring | DELETE | §3 kernel list (absent) + §8 DELETE (dead/zero-yield) | Writes ActivityLog STRATEGIC_VENDOR_EXPIRING warnings for strategic vendors expiring within 7d. DB: zero strategic_vendors rows ever (sequence untouched) and 0 strategic_vendor_expiring activity_log rows ever. |
| webhook_subs | DELETE | §3 kernel-only; §8 delete (dead machinery) | Manages Graph mail/Teams push subscriptions (webhook_service renew/ensure). DB: graph_subscriptions 0 rows EVER despite activity_tracking_enabled=true and 5-min schedule — the BUYER/SALES/TRADER role filter excludes the only co… |
