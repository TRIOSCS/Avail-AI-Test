# MIGRATION_NUMBERS_IN_FLIGHT — alembic migration-number coordination log.
#
# Why: numbered migrations are claimed manually, and two open branches that both pick
# the next free number (each chaining onto the same parent) create two alembic heads
# the moment the second one merges — `alembic upgrade head` then fails at deploy time.
# tests/test_migration_chain.py turns that into a CI failure on whichever branch lands
# second, but only AFTER the collision exists; this file is the discovery path that
# prevents it (a branch author cannot otherwise know which numbers are in flight
# without trawling every open PR).
#
# Protocol (checked by tests/test_migration_numbers_in_flight.py):
#   1. Before creating alembic/versions/NNN_*.py, take the lowest 3-digit number not
#      listed below and not already present in alembic/versions/.
#   2. Append a claim line in the SAME PR/commit that adds the migration:
#         NNN  <branch-name>  <one-line description>
#   3. Claim lines are append-only and permanent — never renumber or delete one. If
#      the later-merging branch finds its parent superseded (a newer migration landed
#      on main first), re-chain its down_revision onto the new head (or add an
#      `alembic merge heads` revision); the claimed number stays the same.
#   Two branches claiming the same number now collide HERE (a git conflict on the same
#   appended line, or the duplicate-number test) instead of at deploy time.
#
# Format: <3-digit number> <branch> <notes...>  — lines starting with '#' are ignored.
097 feat/dual-brand-filters dual-brand columns (PR #263, open — RE-CHAINED: down_revision now 098_materials_perf_idx, chain runs 096 -> 098 -> 097; number kept per protocol)
098 perf/materials-post-ingest materials perf indexes (PR #262, MERGED to main)
099 feat/on-add-enrichment on-add enrichment conflicts (PR #264, open — RE-CHAINED: #263 merged first, so down_revision now 097_dual_brand; chain runs 096 -> 098 -> 097 -> 099; single head verified via `alembic heads`)
100 feat/ladder-monopoly distributor-taxonomy alias backfill (post-093 aliases hard drives/internal hard drives -> hdd, memory module(s) -> dram; data-only, no-op downgrade)
101 feat/oem-web-resolution oem_crosswalk table (PartSurfer/PSREF spare->canonical cache incl negative rows — RE-NUMBERED from 100: feat/ladder-monopoly claimed 100 and merged first; re-chained onto 100_taxonomy_alias_backfill, chain runs 096 -> 098 -> 097 -> 099 -> 100 -> 101)
102 feat/vendor-part-unavailability vendor_part_unavailability table (PR #270; RE-NUMBERED 097->101->102: feat/oem-web-resolution claimed 101 and merged first; chained onto 101_oem_crosswalk)
103 feat/vendor-part-unavailability unavailability temporal-policy + provenance columns (PR #270; RE-NUMBERED 098->102->103 alongside the table migration; chains onto 102_vendor_part_unavailability)
104 feat/trust-architecture reconcile_runs + facet_audits telemetry tables (plan reserved 102, but #270 claimed 102/103 and merged first — took the next free number per protocol; chains onto 103_unavail_policy_columns)
105 feat/worker-lane-demand demand telemetry columns (sourced_qty_90d, last_sourced_at) + ix_mc_demand_queue (task brief said "claim 103" but PR #270 merged first holding 102+103 and the sibling feat/trust-architecture claimed 104; RE-CHAINED: 104_trust_telemetry merged to main first, so down_revision now 104_trust_telemetry; chain runs 103 -> 104 -> 105, single head verified via `alembic heads`)
106 fix/brand-canonicalization manufacturers-table HPE/TI alias merge, data-only (RE-NUMBERED from 104: trust-architecture took 104, worker-lane-demand 105; RE-CHAINED onto 105_demand_telemetry at merge — chain runs 103 -> 104 -> 105 -> 106, single head verified via `alembic heads`)
107 feat/search-part-dossier is_scratch flag on requisitions (scratch/quick-source reqs for the Search Part Dossier); chains onto 106_brand_canonicalization, single head verified via `alembic heads`
108 worktree-buy-plan-audit-fixes drop dead token-approval columns (approval_token/token_expires_at/ix_bpv3_token) + add buy_plan_lines.last_nudge_at + ix_bpl_nudge_status for the unconfirmed-instruction nudge job; RE-CHAINED onto bp_cph_recorded_at at merge (#343 landed on main first; both originally chained off 107). Chain 107 -> bp_cph_recorded_at -> 108, single head verified via `alembic heads`
109 fix/inflow-offer-feed make offers.requisition_id nullable + SET NULL FK (unsolicited inbound vendor emails become proactive-eligible Offers); chains onto 108_buyplan_audit_fixes
110 worktree-crm-redesign-cockpit crm cadence two-clock columns + account tier (RE-NUMBERED from 108: buy-plan-audit-fixes claimed 108 and inflow-offer-feed claimed 109, both merged to main first; RE-CHAINED onto 109_offer_nullable_requisition — single head verified via `alembic heads`)
111 feat/auto-datasheet-capture material_card_datasheets + card datasheet stamps (RE-NUMBERED 108->110->111: buyplan-audit(108)/offer-feed(109)/crm-cadence(110) landed on main first; RE-CHAINED onto 110_crm_cadence_clocks; single head verified via `alembic heads`)
112 feat/offer-qualification-capture offer qualification capture columns (qualification_status/qualification_note/qualification JSON) + condition used->pulls (PR #356; RE-NUMBERED 108->111->112: buyplan-audit/offer-feed/crm-cadence/auto-datasheet all merged to main first; RE-CHAINED onto 111_material_card_datasheets — single head verified via `alembic heads`)
113 sp4-quote-attribution add quotes.source for proactive revenue attribution; chains onto 112_offer_qualification
114 feat/p1b-send-time-clock contacts.sent_at for durable send-time tracking; chains onto 113_quote_source
115 feat/p1c-subscription-health GraphSubscription health columns (last_renewed_at, renew_fail_count, last_error); chains onto 114_contact_sent_at
116 feat/p2d-do-not-contact site_contacts.do_not_contact (Boolean NOT NULL server_default false); chains onto 115_subscription_health
117 feat/datasheet-company-library material_card_datasheets.library_drive_id (RE-NUMBERED 116->117: 116_site_contact_dnc landed on main first; RE-CHAINED onto 116_site_contact_dnc)
118 feat/crm-disposition Company.disposition(active/bucket)+disposition_reason/set_by/set_at + site_contacts.is_priority/is_archived (Boolean NOT NULL server_default false); chains onto 117_datasheet_library_drive_id
119 worktree-comm-ledger-alerts alert_seen table (per-user cross-app alert read-state) for the AlertSource primitive; RE-NUMBERED 117->118->119: feat/datasheet-company-library(117) + feat/crm-disposition(118) merged to main first; RE-CHAINED onto 118_company_disposition, single head verified via `alembic heads`
120 feat/crm-aiorg Company.normalized_name(pg_trgm GIN, Postgres-guarded)+alternate_names JSON; backfill from name; chains onto 119_alert_seen
121 chore/datasheet-library-column-rename rename material_card_datasheets onedrive_item_id->library_item_id + onedrive_url->library_web_url (stale OneDrive-era names; pure rename done while table empty pre-deploy); RE-NUMBERED 120->121: feat/crm-aiorg(120) merged first; RE-CHAINED onto 120_company_name_matching, single head verified via `alembic heads`
122 feat/sp3-ai-screening prospect_accounts.trio_match_score + opportunity_score (Integer indexed); RE-NUMBERED 120->121->122 (crm-aiorg took 120, datasheet-library-column-rename took 121, both merged first); RE-CHAINED onto 121_datasheet_lib_col_rename, single head verified via `alembic heads`
123 feat/sp4-account-reclamation park provenance columns (swept_from_owner_id, swept_at, parked_by_id) on prospect_accounts; chains onto 122_prospect_ai_scores
124 fix/offer-status-constraint reconcile offers status CHECK with OfferStatus enum: drop drifted chk_offer_status (omitted approved/sold, phantom lost) + drop/recreate validated ck_offers_status; chains onto 123_sp4_park_provenance
125 worktree-enrichment-blending-explorium-clay companies+vendor_cards: ticker/naics/revenue_range/enrichment_provenance; chains onto 124_offer_status_constraint
126 feat/attachments-unified unified file attachments: rename onedrive_item_id->library_item_id + onedrive_url->library_web_url + add library_drive_id on requisition/requirement/offer_attachments; create company_attachments + site_contact_attachments + material_card_attachments (unified schema, library_drive_id NULL => user-OneDrive fallback); chains onto 125_enrichment_provenance
127 worktree-resell-trading-module ADDITIVE trading schema: create excess_offers + excess_offer_lines, add excess_line_items.{material_card_id,best_offer_unit_price,best_offer_id,offer_count} + excess_lists.version; NO drops (Bid/BidSolicitation retired in a later cutover chunk); RE-NUMBERED 126->127 (attachments-unified took 126); RE-CHAINED onto 126_unified_attachments; single head verified via `alembic heads`
128 worktree-resell-trading-module ADDITIVE bid-back schema (Chunk E): create customer_bids + customer_bid_lines, add excess_lists.{open_at,close_at}; NO drops; RE-CHAINED onto 127_trading_additive_schema
129 worktree-resell-trading-module CUTOVER (Chunk D): DROP bids + bid_solicitations (old excess concept fully replaced by ExcessOffer/CustomerBid); downgrade recreates both tables structure-only (schema-reversible, no data restore); RE-CHAINED onto 128_bid_back_schema
130 feat/crm-phone-matching normalized_phone (E.164) columns + indexes on Company.phone / SiteContact.phone / CustomerSite.contact_phone(_2) / VendorContact.phone + normalized_phones JSON on VendorCard.phones; backfill via phonenumbers
131 feat/tbf-browser-worker tbf_search_queue + tbf_search_log + tbf_worker_status (singleton id=1 seeded); RE-NUMBERED 130->131 (feat/crm-phone-matching took 130, landed on main first); RE-CHAINED onto 130_phone_normalization; single head verified via `alembic heads`
132 feat/crm-editability custom_fields JSONB on companies + site_contacts
133 feat/resell-outreach ADDITIVE resell-outreach tracking schema: create excess_outreach (buyer x line tracking spine) + buyer_scores (1:1 per vendor_card buyer-engagement rollup), add activity_log.excess_list_id (nullable SET NULL scope + partial index); NO drops; RE-NUMBERED 132->133 (feat/crm-editability took 132); RE-CHAINED onto 132_crm_custom_fields; single head verified via `alembic heads`
136  feat/crm-steps-3456  Company.primary_contact_id (FK site_contacts) + parent_company_id (self-FK companies); RE-NUMBERED 133->136 (feat/resell-outreach took 133, merged first); RE-CHAINED onto 133_resell_outreach_schema
137  feat/crm-steps-3456  site_contacts.first_name/last_name (split from full_name, backfilled) + contact_owner_id (FK users); RE-NUMBERED 134->137; RE-CHAINED onto 136_company_links
138  feat/crm-steps-3456  RequisitionTask.requisition_id nullable + company_id/site_contact_id FKs + CHECK >=1 parent (general account/contact tasks); RE-NUMBERED 135->138; RE-CHAINED onto 137_contact_fields
139  fix/contact-active-visibility  site_contacts.is_active server_default=true + NULL backfill; chains onto 138_general_tasks; single head verified via `alembic heads`
140  feat/ownership-p3-collaborators  account_collaborators table (Phase 3: helper collaborator access); chains onto 139_contact_is_active_default; single head verified via `alembic heads`
141  feat/ownership-p4-compliance  prospect_accounts.reclaim_blocked_until (Phase 4: 30-day reclaim cooldown after sweep; managers bypass via reassign); chains onto 140_account_collaborators; single head verified via `alembic heads`
142  feat/rubric-h1-vendor-parity  vendor_card_id + vendor_contact_id on requisition_tasks + extended ck_task_has_parent + vendor task routes; chains onto 141_reclaim_cooldown; single head verified via `alembic heads`
143  feat/rubric-h1-vendor-parity  vendor_card_attachments + vendor_contact_attachments (migration 143); chains onto 142_vendor_task_cols; single head verified via `alembic heads`
144  feat/rubric-d-contact-fields  site_contacts.secondary_email + secondary_phone + reports_to_id (self-FK SET NULL) + EntityTag entity_type='site_contact'; chains onto 143_vendor_attachments; single head verified via `alembic heads`
145  feat/rubric-h2-vendor-parity-p1  VendorContact.is_primary (Boolean NOT NULL server_default false) + VendorCard.custom_fields (JSONB default {}); chains onto 144_contact_fields; single head verified via `alembic heads`
146  feat/rubric-e-tasks-deal  requisitions.win_probability (Integer nullable 0-100); chains onto 145_vendor_parity_p1; single head verified via alembic heads
147  feat/rubric-d2-audit-trail  companies+customer_sites+site_contacts: created_by_id+modified_by_id (FK users.id ondelete=SET NULL, nullable); chains onto 146_req_win_probability; single head verified via alembic heads
148  feat/archive-dnc  customer_sites.do_not_contact (Boolean NOT NULL server_default false) + archive-DNC feature; chains onto 147_crm_audit_trail; single head verified via alembic heads
149  feat/phase0-isactive  companies+customer_sites+users.is_active: backfill NULL->true + server_default=true + NOT NULL (pre-import data hardening, same class as 139); chains onto 148_site_dnc; single head verified via alembic heads
150  worktree-user-management  user-management foundation: users.{last_login_at,access_overrides(JSON server_default '{}'),invited_by_id(FK users.id SET NULL)} + user_admin_audit table (actor SET NULL, target CASCADE, indexes on target_user_id+created_at); RE-NUMBERED 148->149->150 on collision, chains onto 149_is_active_defaults; single head verified via alembic heads
151  feat/settings-refine  users.notify_buyplan_email_enabled + notify_new_offer_alert_enabled (Boolean NOT NULL server_default true) for Profile-tab notification toggles; RE-NUMBERED 149->151 on collision (feat/phase0-isactive took 149, worktree-user-management took 150, both merged first); RE-CHAINED onto 150_user_mgmt; single head verified via alembic heads
152 feat/partsurfer-negative-cache partsurfer_desc_negative table (durable negative cache for PartSurfer DESCRIPTION no-result/ungrammatical misses; spare_norm-keyed, 90d/14d retry windows); RE-NUMBERED 125->152 (125_enrichment_provenance + chain through 151 landed on main first); RE-CHAINED onto 151_user_notify_prefs, single head verified via `alembic heads`
153 fix/quote-send-correctness quotes.{graph_message_id,graph_conversation_id} (nullable String) — capture the sent quote-email's Graph message/conversation id for customer-reply threading; chains onto 152_partsurfer_desc_negative; single head verified via `alembic heads`
154 fix/drift-reconcile-464 schema-drift reconciliation (#464): drop dead activity_log.source_url + vendor_responses.teams_alert_sent_at columns and the redundant fk_activity_log_quote FK; retires the matching grandfathered entries in scripts/check_schema_matches_models.py so the gate enforces them; chains onto 153_quote_graph_ids; single head verified via `alembic heads`
155 feat/roles-manager-approval data-sweep legacy role 'ops'->'manager' (UserRole renamed long ago; reversible best-effort) + users.can_approve_buy_plans (Boolean NOT NULL server_default false) for the per-user buy-plan approval right; RE-CHAINED onto 154_drop_dead_cols at merge (#464 landed on main first; number 155 kept per protocol); single head verified via `alembic heads`
156 feat/profile-avatar users.avatar_path (nullable String(255)) for the Profile-tab photo uploader; serves via /api/user/avatar/{filename}; RE-CHAINED onto 155_roles_manager at merge (154/155 landed on main first; number 156 kept per protocol); single head verified via `alembic heads`
157 feat/qp-approvals-phase1 QP + Approvals Engine schema: 8 new tables (approval_gate_configs, prepayments, quality_plans, approval_requests, approval_events, approval_outbox, approval_steps, approval_step_recipients) + 6 new Offer columns (is_primary, sourcing_type, vendor_rating, terms, location, specifics); chains onto 156_user_avatar; single head verified via `alembic heads`
158 feat/sales-hub-status requisitions.outcome_reason (Text nullable; required Won/Lost close reason, enforced app-side) + status remap archived->lost / active+sourcing+reopened->open / quoting->quoted + ck_requisitions_status CHECK whitelist (draft/open/rfqs_sent/offers/quoted/won/lost/hotlist/cancelled); NO requisition archive/hide capability; RE-NUMBERED 157->158 + RE-CHAINED onto 157_qp_approvals (157 taken by QP/Approvals, merged first); single head verified via `alembic heads`
159 refactor/approvals-engine-cleanup approvals-engine foundation cleanup before QP Phase C: approval_requests polymorphic (subject_type, subject_id) pair + composite ix_approval_req_subject replacing the two subject FK cols/indexes; approval_outbox.channel server_default email->in_app; drop dead approval_events.note; reversible (downgrade reconstructs FK cols/indexes, EXISTS-guarded backfill); chains onto 158_req_pipeline_hotlist; single head verified via `alembic heads`
160 feat/qp-c2a-so-po-gates QP Phase C2a: users.can_approve_sales_orders + can_approve_pos (Boolean NOT NULL server_default false) for the per-user QP Sales/Purchasing section approval rights (SALES_ORDER/PURCHASE_ORDER gates); additive/reversible; chains onto 159_approval_subject_poly; single head verified via `alembic heads`
161 feat/qp-c2b-native-sections QP Phase C2b: quality_plans.sales_*/purchasing_* native section columns (28 nullable Quality-Questions fields) + sales_section_approved_at/purchasing_section_approved_at (UTCDateTime) + qp_serial_entries + qp_fru_lookups child tables (FK qp_id CASCADE; uq_qp_fru_lookup on (qp_id,fru_norm)); additive/reversible, round-tripped on a throwaway PG (staging untouched); chains onto 160_qp_so_po_approvers; single head verified via `alembic heads`
162 feat/po-resource-vendor-cancel po_cancellations table (immutable one-row-per-cancelled-PO vendor-fall-down fact powering cancellation frequency + days-to-cancel metrics; FKs SET NULL + denormalized vendor key) + vendor_cards.{avg_days_to_cancel,slow_cancel_count} + vendor_metrics_snapshot.avg_days_to_cancel + users.notify_resource_alert_enabled (Boolean NOT NULL server_default true — re-source urgent-broadcast opt-out); additive/reversible, round-tripped upgrade->downgrade->upgrade on a throwaway PG (staging untouched), drift checker clean; chains onto 161_qp_native_sections; single head verified via `alembic heads`
163 feat/approvals-module-sp2-sales-order  SP-2: buy_plans_v3.quote_id->nullable ONLY (standalone, DEPLOYED to staging). The rest of the SP-2 schema (qp_sales column/gate rename + sales_so_number retire) lives in 164. down_revision=162_resource_and_cancellations
164 feat/approvals-module-sp2-sales-order  SP-2 remaining schema (chained after deployed 163): users.can_approve_sales_orders->can_approve_qp_sales (Op A); approval_requests.gate_type sales_order->qp_sales (Op B, Task 5); quality_plans.sales_so_number backfill->buy_plan + drop (Op C, Task 7). down_revision=163_sp2_sales_order_gate
167 feat/crm-p4-power-ux  saved_views table (per-user CRM list filter presets: id/user_id FK->users CASCADE/list_key/name/filters JSON/created_at; uq_saved_view_user_key_name on (user_id,list_key,name) + ix_saved_views_user_key). Additive/reversible. 165 & 166 reserved per task; chains onto 164_sp2_qp_sales_rename; single head verified via `alembic heads`
165 feat/vendor-soft-archive  vendor_cards.is_active (Boolean NOT NULL server_default true) + ix_vendor_cards_is_active for vendor soft-archive (hidden from default list, never deleted; mirrors Company.is_active 139/149); chains onto 164_sp2_qp_sales_rename; single head verified via `alembic heads`
166 feat/approvals-sp3-po-receiving SP-3: (A data) approval_requests.gate_type purchase_order->qp_purchasing WHERE subject_type='quality_plan' (QP-purchasing de-collision; deal-level purchase_order kept); (B rename) users.can_approve_pos->can_approve_qp_purchasing; (C add) users.can_approve_purchase_orders (Bool server_default false) + purchase_order_approval_limit (Numeric(12,2) nullable) for the deal-level PO gate. BuyPlanStatus.INBOUND is a free String(30) — no DDL. additive/reversible, round-tripped upgrade->downgrade->upgrade on a throwaway PG (staging untouched); chains onto 165_vendor_is_active; single head verified via `alembic heads`
168 feat/persist-enriched-today-cap enrichment_worker_status.enriched_today_date (nullable Date) — tags the persisted enriched_today daily-cap count with its UTC date so a same-day container restart RESUMES the count (cap stays enforced) instead of resetting the budget; 165/166/167 reserved; additive/reversible; chains onto 164_sp2_qp_sales_rename; single head verified via `alembic heads`
169 feat/crm-p5-trust  crm_field_history table (per-record CRM field-DIFF audit trail; polymorphic entity_type 'company'|'contact' + entity_id, old/new/field/changed_by/created_at, ix_crm_field_history_entity) for the account History tab + contact History modal. Additive/reversible; round-tripped upgrade->downgrade->upgrade on a throwaway PG (staging untouched). 168/165 already on main; chains onto 167_saved_views (current head); single head verified via `alembic heads`
170 feat/prospecting-persistence  prospect_accounts.buyer_ready_score (Integer nullable) write-through CACHE of build_priority_snapshot's composite score (kept in lockstep via ProspectAccount before_insert/before_update listener; backfilled in-migration) + ix_prospect_accounts_buyer_ready_score btree so the prospecting buyer_ready_desc sort ranks in SQL; PLUS one new Postgres-only pg_trgm GIN index ix_sightings_vendor_email_trgm for the warm-intro leading-wildcard ILIKE lookup (detect_warm_intros) — the lookup's other ILIKE column site_contacts.email is ALREADY trgm-indexed by a513288799de, so 170 does NOT touch it. Additive/reversible; round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG (staging untouched); ix_sightings_vendor_email_trgm added to drift-checker grandfathered remove-index set; chains onto 169_crm_field_history; single head verified via `alembic heads`
171 feat/unavailability-v2-condition-aware  vendor_part_unavailability.condition (String(16) nullable; NULL=all-conditions catch-all) + replace uq_vendor_part_unavail_vendor_mpn with two PARTIAL unique indexes uq_vpu_vendor_mpn_condition (WHERE condition IS NOT NULL) and uq_vpu_vendor_mpn_allcond (WHERE condition IS NULL); additive/reversible; chains onto 170_prospecting_persistence; single head verified via alembic heads
172 fix/drift-reconcile-464-indexes  schema-drift index reconciliation (#464): drop the redundant duplicate ix_requisitions_company_id index (the model-declared ix_requisitions_company from 001 stays); PLUS declare ~35 pre-existing raw-DDL indexes (pg_trgm GIN, GIN-on-JSONB tag arrays, FTS tsvector GIN, plain btree FK, simple partial) directly on the ORM models so autogenerate sees them, and retire their grandfathered entries in scripts/check_schema_matches_models.py + rename site_contacts reports_to index=True -> explicit ix_sc_reports_to. NO data change; round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG (staging untouched); chains onto 171_unavail_condition; single head verified via `alembic heads`
173 feat/approvals-rework-workflow-fold  Phase D workflow fold (DATA-ONLY, no DDL): (a) R2 SO-fold backfill UPDATE buy_plans_v3 SET so_status=approved, so_verified_at=now() WHERE status IN (active,inbound) AND so_status=pending; (b) R3 PO-rights grandfather UPDATE users SET can_approve_purchase_orders=TRUE for active verification_group_members. Both irreversible -> documented no-op downgrade; must precede code on deploy. chains onto 172_drop_dup_req_company_idx; single head verified via `alembic heads`
174 chore/drift-reconcile-464-uniques  #464 finish: add the 21 grandfathered unique constraints (all dupe-checked clean on live PG 2026-07-02) + DROP IF EXISTS 3 model-less legacy tables (buy_plans V1/notification_engagement/self_heal_log — absent on live staging, no inbound FKs; downgrade recreates from pg_dump DDL) + material_cards.enrichment_status column comment (model+migration aligned); prune matching grandfather entries; additive/reversible; round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG (staging untouched); chains onto 173_approvals_workflow_fold
175 worktree-wf_7eead189-b44-2  OQ-02/REQ-04 bulk cross-req -> ONE combined quote: new quote_requisitions join table (quote_id FK CASCADE + requisition_id FK CASCADE, uq_quote_requisition on (quote_id,requisition_id) + ix_quote_requisitions_quote + ix_quote_requisitions_req) so a combined quote links every contributing requisition while Quote.requisition_id stays the primary/anchor; BACKFILL in-migration (INSERT ... SELECT id, requisition_id, COALESCE(created_at, now()) FROM quotes) so every existing single-req quote gets its self-row and all read paths use ONE join helper; additive/reversible, round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG (staging untouched); chains onto 174_reconcile_uq_drift; single head verified via `alembic heads`
176 feat/phase3-stage3-po-gate-retire  retire the deal-level PURCHASE_ORDER gate (Phase-3 stage-3, DATA-ONLY no DDL): (a) UPDATE approval_requests status requested->cancelled + resolved_at=now() + explanatory resolution_note WHERE gate_type='purchase_order' AND subject_type='buy_plan' (QP-purchasing rows untouched — 166 already renamed them; resolved history kept); (b) UPDATE buy_plans_v3 status inbound->active (release plans parked in the retired SP-3 holding state). Irreversible -> documented no-op downgrade (mirrors 100/173); must precede stage-3 code on deploy; round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG (staging untouched); chains onto 175_add_quote_requisitions; single head verified via `alembic heads`
177 feat/phase3-stage2a-qp-fold  QP lightweight fold (decision C, DDL reversible): RENAME quality_plans.sales_section_approved_at->sales_section_reviewed_at + purchasing_section_approved_at->purchasing_section_reviewed_at (hand-written ALTER RENAME COLUMN so timestamps survive); ADD sales_section_reviewed_by_id + purchasing_section_reviewed_by_id (Integer FK users.id ondelete SET NULL nullable); DROP dead approved_by_id/approved_at (downgrade recreates as nullable per 157). Round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG (staging untouched); chains onto 176_retire_deal_po_gate; single head verified via `alembic heads`
178 feat/prepayment-po-workflow  Prepayment.buy_plan_line_id (Integer FK buy_plan_lines.id ondelete SET NULL, nullable) + fk_prepayment_buy_plan_line + ix_prepayment_buy_plan_line — link a prepayment to the specific PO line it prepays. Additive/reversible (downgrade drops index->FK->column); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG (staging untouched); chains onto 177_qp_section_reviewed_cols; single head verified via `alembic heads`
179 feat/prepay-closure  Prepayment payment lifecycle: status (String(20) NOT NULL server_default 'requested', ix_prepayment_status) + approve stamps (approved_by_id FK users.id SET NULL, approved_at UTCDateTime) + pay_token (String(64), unique ix_prepayment_pay_token) + paid group (paid_at, paid_by_id FK users.id SET NULL, paid_by_label, paid_via, wire_reference, paid_amount Numeric(12,2)) + void group (voided_at, voided_by_id FK users.id SET NULL, void_reason); BACKFILL status from PREPAYMENT ApprovalRequest (approved->approved+approved_at from resolved_at; rejected->void+voided_at+void_reason; approval_requests has no resolver-user col so approved_by_id/voided_by_id left NULL); additive/reversible (downgrade drops FKs->indexes->13 columns); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG (staging untouched); chains onto 178_prepayment_line_link; single head verified via `alembic heads`
180 feat/ticket-kind-discriminator  trouble_tickets.ticket_type (String(20) NOT NULL server_default 'bug') + ix_trouble_tickets_ticket_type — bug/feature discriminator for the unified ticket system; existing rows backfill 'bug' via server_default. Additive/reversible (downgrade drops index->column); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG (staging untouched); chains onto 179_prepay_closure; single head verified via `alembic heads`
181 main  users.display_timezone (String(64) nullable) — per-user IANA DISPLAY timezone (browser auto-detect + profile override) so stored-UTC timestamps render in each viewer's zone; distinct from the Graph mailbox users.timezone (Windows format). Additive/reversible (downgrade drops the column); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); chains onto 180_ticket_kind_discriminator; single head verified via `alembic heads`
182 main  requisition_tasks: DROP ai_priority_score (Float) + ai_risk_flag (String(255)) — dead AI-scoring columns written by nothing (score_tasks_with_ai/apply_simple_scoring removed as dead code; sole readers always saw NULL). Additive-reverse (downgrade recreates both nullable, no data to restore); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); chains onto 181_add_user_display_timezone; single head verified via `alembic heads`
183 main  customer_bids.sent_at + responded_at (UTCDateTime nullable) + responded_by_id (Integer FK users.id ondelete SET NULL, fk_customer_bids_responded_by) for the M4 bid-back send/accept/reject lifecycle who/when audit trail (status + revision already exist); additive/reversible (downgrade drops FK then 3 cols); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); chains onto 182_drop_task_ai_columns; single head verified via `alembic heads`
184 main users.reports_to_id (Integer self-FK users.id ondelete SET NULL, nullable, fk_users_reports_to) — per-rep manager/supervisor so account-park alerts route to THIS manager (else all-managers fallback); distinct from users.invited_by_id; additive/reversible (downgrade drops FK then column); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); chains onto 183_customer_bid_lifecycle; single head verified via `alembic heads`
185  main requirements.outcome_reason (Text nullable) — per-part "why won / why lost" close reason captured by the Sales-Hub Mark Won / Mark Lost bulk action (the part-line replacement for the removed bulk Archive); app-enforced non-empty on WON/LOST transition, mirrors requisitions.outcome_reason (158). Additive/reversible (downgrade drops the column); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); chains onto 184_user_reports_to; single head verified via `alembic heads`
186  main offers: DROP 6 dead QP/Approval "Task-2" additive columns (is_primary, sourcing_type, vendor_rating, terms, location, specifics) — added by 157, never wired to any writer/reader (grep-verified across app/+templates/+schemas/; is_primary always its 'false' default, other 5 always NULL). Dead SourcingType enum + RiskFlagSeverity enum + QualityPlanStatus in_review/approved/rejected + QPOrderType.revision removed as code-only (String-backed cols, no CHECK). Additive-reverse (downgrade recreates all 6 as 157 defined them, nullable, no data to restore); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); chains onto 185_req_outcome_reason; single head verified via `alembic heads`
187  main  P2.7 startup/health-check decoupling: 8 PostgreSQL partial indexes on the exact IS NULL predicates app/startup.py's deferred backfills scan every boot (requirements/material_cards/sightings/offers.normalized_mpn, sightings/offers.vendor_name_normalized, trouble_tickets risk_tier+category, prospect_accounts sweep-cooldown) so repeat-boot scans are O(remaining rows) not O(table); no-op on SQLite (dialect-guarded). Concurrently claimed by 71d3fef96529 (feat/rubric... requirements.assigned_buyer_id index, P3.1, hex revision id — not registry-tracked); merged via `alembic merge heads` into 1223a56cbbbb (single head verified). Round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (scratch db, not staging).
188  fix/714-deferrals-fk-and-domains  canonicalize FK on offers.excess_line_item_id -> single offers_excess_line_item_id_fkey (PG default / model name): drop the duplicate fk_offers_excess_line_item_id that d1a2b3c4e5f6's name-scoped replay guard created on fresh-DB chain replays (reproduced on throwaway PG 16 — BOTH names present after upgrade head), or RENAME it to canonical on pure-incremental DBs that only have the legacy name; d1a2b3c4e5f6 + 5c6736d6381f guards fixed to COLUMN-scoped so the dup can never be recreated; PG-only (dialect-guarded no-op on SQLite); irreversible cleanup -> documented no-op downgrade (mirrors 100/173/176); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); chains onto 187_startup_backfill_partial_idx; single head verified via `alembic heads`
189  fix/category-residue-backfill-189  DATA-ONLY category residue remap (no DDL): three passes over material_cards.category on LOWER(TRIM(category)) — (a) 64 NEW 2026-07 residue aliases (frozen from category_normalizer.CATEGORY_ALIASES, added same PR) e.g. "schottky diodes & rectifiers"->diodes, "8-bit microcontrollers - mcu"->microcontrollers; (b) re-run 8 already-aliased strings that leaked back after 093/100 (pre-F1-ladder writers bypassed the forward hook), current targets == 093 targets so nothing strands; (c) lowercase case-variants of canonical keys (093 step-1b style). Soft-deleted rows included, provenance columns untouched; 17 ambiguous residue strings intentionally untouched (backlog row). Irreversible many-to-one -> documented no-op downgrade (mirrors 093/100); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); chains onto 188_canonical_offers_excess_fk; single head verified via `alembic heads`
191  fix/account-type-index  Add btree index ix_companies_account_type on companies.account_type — actively filtered (no index) in app/services/crm_service.py::list_companies (Company.account_type == account_type for CDM types) and app/services/alerts/sources/inbound_customer.py (== "Customer"); plain single-column btree, additive + PG-safe (no rewrite). Registered in Company.__table_args__ (app/models/crm.py) so the schema-drift gate stays green. Reversible (downgrade drops the index); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); RE-CHAINED at merge: down_revision now 190_admin_bootstrap_optout (190 landed on main first); number kept per protocol; single head verified via `alembic heads`
190  fix/p1-admin-optout  users.admin_bootstrap_opted_out (Boolean NOT NULL server_default false) — latch a demoted ADMIN_EMAILS bootstrap admin's opt-out so the routers/auth.py login bootstrap does not silently re-promote them on next login (set True on admin->non-admin demotion in admin/users.change_user_role, cleared on re-promote to admin); additive/reversible (downgrade drops the column); server_default backfills existing rows to false during the NOT NULL add; round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (staging untouched); chains onto 189_category_residue_backfill; single head verified via `alembic heads`
193  feat/resell-lifecycle-guards  DATA-ONLY legacy ExcessList status remap (no DDL): three case-insensitive UPDATEs on excess_lists.status (LOWER(TRIM(status)), mirroring 189) — active->open (+ stamp open_at where NULL via COALESCE), bidding->collecting (+ stamp open_at where NULL), closed->closed (casing normalize ONLY; D5 keeps CLOSED distinct from bid_out). Order-coupled with the publish guard (same PR): once no row is in a legacy status, publish_list can reject any non-DRAFT publish. status is plain String(20), no check constraint, so a pure data UPDATE. SQL factored into remap_legacy_statuses(conn) so tests drive identical code. Irreversible many-to-one -> documented no-op downgrade (mirrors 093/100/189); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 + seeded-row data check (staging untouched); 192 reserved by feat/approvals-workspace so this took 193; chains onto 191_companies_account_type_index; single head verified via `alembic heads`
194  feat/resell-outreach-truthfulness  Resell Phase 2 outreach send-truthfulness: ADD excess_outreach.send_error (Text, NULLABLE) — persisted send-failure reason for the new ExcessOutreachStatus.FAILED / INTERRUPTED members (status is plain String(20) with no check constraint + app-side @validates, so the two enum members need NO DDL). Additive/reversible (downgrade drops the column); round-tripped upgrade->downgrade->upgrade on a THROWAWAY PG 16 (p2check, staging untouched); chains onto 193_resell_legacy_status_remap; single head verified via `alembic heads`
195  feat/resell-outreach-truthfulness  ADD excess_outreach.send_subject + send_body (Text, NULLABLE) — persist the EXACT subject/body a resell-outreach email was sent with so the one-click Retry double-send guard (email_service._find_sent_message, EXACT-subject match) matches an already-delivered CUSTOMIZED-subject campaign instead of resending it, and a legitimate resend reuses the original wording (not the seeded default). NULL on manual-log + legacy email rows (retry falls back to the default text). Additive/reversible (downgrade drops both columns); status/enum unchanged; chains onto 194_excess_outreach_failed_states; single head verified via `alembic heads`
